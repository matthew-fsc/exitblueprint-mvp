# 06 - Decision Log

Append-only. One line per decision: date, decision, reason. Claude Code adds entries when a session makes a call; Matthew adds entries when product behavior is decided.

- 2026-07-06 | Deterministic rule-based scoring, AI narrative-only | Explainability and reproducibility are credibility requirements with advisors; avoids hallucination risk with financial clients.
- 2026-07-06 | Rubric stored as data with rubric_versions | Methodology will evolve; old assessments must stay comparable against their version.
- 2026-07-06 | Engagement (not deal) as core unit; immutable assessment snapshots | Clients are 12-36 months pre-deal; longitudinal score data is the compounding asset.
- 2026-07-06 | Multi-tenant RLS by firm from S2 | Advisor firms are the distribution channel; retrofitting tenancy is high-risk.
- 2026-07-06 | Advisor workspace before owner portal | Advisors drive adoption and revenue; owners can be served manually during pilot.
- 2026-07-06 | Sub-scores are a first-class table between questions and dimensions | DRS methodology scores derived metrics (HHI, CAGR, ratios), not raw answers; matches Blueprint II exactly.
- 2026-07-06 | DRS = six business dimensions only; owner-side questions form separate ORI | Per Blueprint II composite formula; owner/business divergence is itself a diagnostic output.
- 2026-07-06 | Discrete band scoring, no interpolation, in v1 | Hand-verifiability of fixtures; interpolation available later as rubric version bump.
- 2026-07-06 | HHI estimated from top-5 shares in v1 | Exact HHI requires Blueprint I data ingestion; proxy documented in seed README.
- 2026-07-06 | Unknown NRR scores 25 with a not-tracked flag | Not measuring is worse than measuring badly, but not zero; Matthew to ratify.
- 2026-07-06 | EBITDA recast, EV, value-gap dollars deferred to post-v1 | Methodology documented in docs/07; v1 ships scoring, gaps, roadmap, narrative.
- 2026-07-07 | Restricted dev/CI runs migrations on plain Postgres via db/supabase-shim.sql (auth schema, roles, auth.uid()) | Docker/supabase CLI unavailable in the build container; migrations stay Supabase-canonical, shim skipped on real Supabase.
- 2026-07-07 | Engine rounding replicates Python round-half-even on the exact binary double (pyRound) | Reference scorer used Python round; fixture 3 ORI (40.25 -> 40.2) is irreproducible with JS default rounding.
- 2026-07-07 | ORI sub-scores persist to sub_score_results like business sub-scores; fixture sub_scores maps cover business dims only | Explain trace and owner-readiness reporting need the stored parts; matches reference which tracks ORI separately.
- 2026-07-07 | Not-tracked flag text derived from sub-score code suffix (REV-NRR -> "NRR not tracked") | Matches reference fixture output; revisit if flags need per-sub-score copy.
- 2026-07-07 | Dev-only "Phase 1 verification" page runs the pure engine in-browser on bundled seed rubric + fixtures | Visual acceptance evidence for Phase 1; production scoring still reads rubric from db; page carries a DEV label.
- 2026-07-07 | Outcome capture schema (engagement_outcomes, append-only outcome_events) ships in v1 with no UI | The moat dataset cannot be built retroactively; recorded only from advisor-reported facts, never backfilled.
- 2026-07-07 | Assessment immutability preserved via supersede pattern (record_status + linkage, active_assessments read path); no in-place edits, ever | Corrections create a new scored assessment; column named record_status because status tracks the intake lifecycle.
- 2026-07-07 | Deltas are same-rubric-version only; cross-version comparison returns an explicit incomparable marker | Cross-version comparison deferred to a rescore-as-derived-data feature that never mutates the original snapshot.
- 2026-07-07 | Firm/advisor/owner provisioning is CLI-only (scripts/admin.ts) through MVP | Admin UI is out of scope until requested; S5 builds login + advisor shell only.
- 2026-07-07 | Demo tenant is seed data (seed/demo + scripts/seed-demo.ts), distinct from correctness fixtures | Demo tells the longitudinal story (59.9 Needs Work -> 72.3 Sale Ready, 6 gaps resolved); validated against the reference scorer inside the seed script, not the test suite.
- 2026-07-07 | Dev Supabase emulator (dev/supabase-dev-server.ts): Vite middleware implementing the auth/rest/functions surface the app uses, backed by local Postgres with real RLS per request | Same philosophy as db/supabase-shim.sql; app code is untouched supabase-js and runs against real Supabase in deploys; emulator is dev-only (fixed password), never bundled.
- 2026-07-07 | S7 "combined composite" rendered as a paired Business+Owner readout with divergence callout, not a blended number | CLAUDE.md rule 3a forbids mixing score groups; divergence is itself the diagnostic (2026-07-06 entry). A blended composite formula needs Matthew's ratification if ever wanted.
- 2026-07-07 | Narrative service uses claude-opus-4-8 with adaptive thinking; prompts forbid model arithmetic; ANTHROPIC_API_KEY server-side env only | Per docs/04 + S4.5 B2; injectable generator keeps tests key-free; strict numeral post-check regenerates once then fails loudly.
- 2026-07-07 | generated_documents.finalized_at added for edit-before-finalize | AI output stays a labeled draft until the advisor finalizes; print/PDF from the finalized view.
- 2026-07-07 | One-click demo: Codespaces badge on README + npm run dev:demo (scripts/dev-demo.sh boots db, migrations, seeds, demo advisor, Vite) | Instant way to open the local dev build; dev-only fixed password, real RLS via the dev emulator.
- 2026-07-09 | Owner report generates without a Claude API key via a deterministic composer (server/narrative.ts composeOwnerReport) | Rule-based narrative assembled from the same explain trace + gap set the scorer produced — no new numbers, nothing invented (CLAUDE.md rule 1/2 safe). Claude drafts when ANTHROPIC_API_KEY is set; otherwise the composer runs, labeled "Composed from your scores and flagged gaps". Report always generates.
- 2026-07-09 | Plain-language interpretation layer (shared/scoring/interpret.ts) is the single source of truth for turning scores/inputs/triggers into sentences | Used by both the results page and the report composer so they never drift. Adds no numbers and makes no scoring decisions — it phrases the deterministic output. Results page redesigned from a formula/matrix table to plain readings with quality bands, a "how scoring works" tier ladder, and plain gap reasons.
- 2026-07-09 | invokeFunction() unwraps the real edge-function error message instead of the generic "non-2xx status code" | Users (and advisors) see why generation/scoring failed rather than an opaque wrapper.
- 2026-07-09 | Full light + dark theme on a semantic design-token system (src/styles.css, src/lib/theme.tsx) | Every component reads from tokens so both themes stay in sync; deep-navy advisory identity. Theme defaults to system preference, is toggleable in the shell + login header, persists to localStorage, and is set pre-paint by an inline script in index.html to avoid flash. Could not reach exitblueprint.net from the build environment (agent proxy 403) to match the marketing site exactly.
- 2026-07-10 | Intake redesigned for zero-friction data entry (src/pages/IntakePage.tsx + src/lib/intakeFields.ts) while the stored answer format is unchanged | Presentation-only metadata layer (intakeFields.ts) maps question codes to unit adornments ($/%/months/hrs), labeled per-item list rows (top-5 customer shares, four-year revenue), humanized option labels, and 1-5 scale anchors parsed from the prompt. Lists render as labeled rows instead of a comma box; numbers show inline unit affixes and a live "$1,234,000" hint; short option sets render as cards, longer ones as a styled dropdown; numeric-or-unknown gets a "Not tracked" toggle. A progress bar counts answered scored questions and step chips are clickable. Answer values persisted to the DB are still numbers/number arrays/option strings/1-5/"unknown", so scoring is untouched — the e2e walkthrough reproduces company-1 exactly (DRS 82.6, Sale Ready, ORI 79.2).
- 2026-07-10 | Scenario Workbench (src/pages/WorkbenchPage.tsx): advisors edit any answer and every score updates in real time, computed in the browser by the shared engine (explainFromAnswers) — zero server round-trip, byte-for-byte identical to server scoring | Answers are held in browser state as a what-if; the source assessment is never mutated (CLAUDE.md rule 4). "Save as new assessment" persists a scenario as a new immutable assessment (next sequence, same rubric version) and scores it via the existing score-assessment path. The board shows DRS/ORI/dimension/gap deltas vs. the saved baseline. Rubric read client-side via loadEngineRubric (RLS methodology_read makes rubric tables world-readable to authenticated users).
- 2026-07-10 | Intake answer controls extracted to src/lib/answerFields.tsx and shared by intake + workbench | One editor, one Draft/toAnswerValue contract so the two surfaces cannot drift; stored answer format unchanged. Engagement page gains a DRS trajectory across assessments so the longitudinal story is visible in one place.
- 2026-07-11 | F0 design system: forest/green institutional palette on the existing token layer | Retheme of src/styles.css from navy to deep forest (brand #16352a, accent #1f7a52, buttons #1b4a38) plus a typed token module (src/lib/tokens.ts) and one shared formatter (src/lib/format.ts) for scores/deltas/currency/dates. Per-firm branding overrides --accent on client-facing views (one variable swap). Fonts are system stacks (Inter/Inter Tight → system-ui) with tabular numerals — no web-font network dependency, keeping the app offline/CI-safe.
- 2026-07-11 | Five DRS tiers get one fixed color each, validated with the dataviz palette validator, used everywhere (chips, dials, charts, PDFs) | Light: Not Saleable #c0362c, High Risk #e0670f, Needs Work #9a7d0a, Sale Ready #2f9e44, Institutional Grade #0e8f9e (teal, deliberately distinct from brand green). Dark variants brighter for the dark surface. Tiers are an ordinal, always-labeled scale — color is never the sole cue (TierBadge always prints the name), so the warm red/orange/amber CVD adjacency is acceptable; all five pass contrast ≥3:1 with monotonic lightness.
- 2026-07-11 | F0 component library (src/components/ui) + dev gallery at /dev/components | ScoreDial, TierBadge, DeltaChip, TrajectoryChart (signature SVG line+area with tier-colored points, supersede markers, dashed target line), DimensionBars, DataTable (sortable/sticky/loading/empty/error), PageHeader, Card, StatBlock, EmptyState, Timeline, FirmMark, Skeleton, ConfirmDialog, Toast. All token-driven; light+dark verified by screenshot.
- 2026-07-11 | F0 data layer: TanStack Query v5 with a central key registry (src/lib/queries.ts) | Clients, Engagement, Results, Report, and Intake pages migrated off ad-hoc supabase-js reads to typed query hooks; mutations invalidate precise keys. WorkbenchPage retains its bespoke Promise.all bootstrap for now (it drives byte-identical client-side scoring; migrating it was deferred to avoid destabilizing that path). Health/Verify are dev/internal surfaces.
- 2026-07-11 | F1 firm branding: firm_branding table (one row/firm), advisor-writable, firm-member-readable | Migration 20260711000100 (additive). accent_color has a hex CHECK; RLS: firm_member_read (advisors + owners) and advisor_firm_write. rls-test extended (+5 assertions → 21/21): advisor creates/reads own, cannot modify firm B; owner reads own firm branding, cannot write. BrandingProvider overrides --accent for the subtree; FirmMark renders logo-or-initial + name; the advisor's firm is the face, "Powered by Exit Blueprint" is discreet. Applied to the shell header and the owner-report header/footer. Logo stored as URL or data URI (dev has no storage bucket; production can swap to Supabase Storage). Demo seed brands the demo firm as "Cascade Wealth Partners".
- 2026-07-12 | F2 advisor portfolio dashboard is the new home (/); Clients moved to /clients | usePortfolio() assembles one cached view of the firm's book (latest active-completed assessment per engagement, prior-vs-latest delta, sparkline points, open-gap counts, days-since-last staleness) client-side from four RLS-scoped reads — no new view/migration. Stale (≥90d) and negative-delta rows are visually prominent; sort/filter by tier, movement, staleness. scripts/seed-portfolio.ts populates the demo firm with N engagements built from the real fixtures and scored by the engine, so dashboard rows and click-through are genuine (not synthetic score rows). Page container widened to 1140px for table density.
- 2026-07-12 | F3 engagement command view redesign | One page: branded header + outcome status, signature TrajectoryChart with a Competitive-Process-Ready target line (DRS 85, docs/07), current-assessment ScoreDial + DimensionBars, open gaps joined to their mapped playbooks, a compare-any-two panel, an assessments list, and documents/tasks sections (tasks empty until F5). Compare is exposed as a real function: compareAssessments wired into the dev emulator as 'compare-assessments' (authorizes both assessment ids through RLS, then runs on a service connection); the panel renders same-version deltas and the explicit incomparable marker, never a fabricated cross-version number. No mutation of completed assessments from this page.
- 2026-07-12 | F4 branded delta report end-to-end (new doc_type delta_report) | Additive enum migration (ALTER TYPE ADD VALUE, transactional on PG16). Server buildDeltaReportPayload runs compareAssessments(prior, current) where prior is the preceding active-completed assessment; no prior or a different rubric version → Baseline mode (levels, never a fabricated change). Narrative via Claude (prompts/delta_report.v1.md) with the same numeral firewall, or a deterministic composeDeltaReport fallback; counts live in the payload so neither path self-computes a number. Server-side HTML→PDF via Playwright (server/pdf.ts renderDeltaReportHtml + renderReportPdf, chromium resolved from EB_CHROMIUM_PATH) — a 3-section print-first branded layout (firm logo/from-line, DRS movement, dimension delta table, gaps resolved/opened, disclosure + Powered-by footer), exposed as the 'render-delta-pdf' binary function; the frontend downloads the Blob. NOT window.print(). DeltaReportPage: pick the as-of assessment → generate → WYSIWYG branded preview → edit narrative → finalize → download PDF. Verified live: a valid 2-page branded PDF rendered from the demo engagement.
- 2026-07-12 | Advisory Library: one catalog (advisory_library_items) unifying buyer_question / initiative / risk_flag, firing off live persisted scores | Migration 20260712000200 (additive; enums advisory_item_type, advisory_source). Global-vs-tenant decided explicitly (audit debt #8): system-curated rows are GLOBAL (firm_id null, readable by everyone like methodology, service_role-write only); advisor-authored rows are TENANT-scoped (firm_id set, firm-isolated RLS). Partial unique index on code where firm_id is null keeps system seeding idempotent. server/advisory.ts fireAdvisoryItems(db, engagementId) reads the latest completed assessment's dimension_scores + sub_score_results verbatim and surfaces catalog items whose governing score (sub_score_code if set, else dimension_code) is ≤ score_trigger, sorted critical-first then lowest-score-first — descriptive only, never computes or adjusts a score, DRS engine stays byte-identical. Exposed as the 'advisory-items' dev function. Seed: seed/advisory-library.csv, 28 curated system items (10 buyer questions, 10 initiatives, 8 risk flags) wired to the same sub-score triggers as the gap rubric. UI: /library catalog (filter by type/dimension/text, source badges, advisor CRUD for firm items) and /engagement/:id/buyer-lens (three lenses grouped, critical-first, live score vs trigger, expandable preparation/documentation). rls-test extended (+7 → 32/32); tests/advisory.test.ts (+5) locks firing order and firm/global visibility.
- 2026-07-13 | Phase 1 (foundation): financial provenance & verification (answer_provenance) | Migration 20260713000100 (additive; enum provenance_source self_reported|document|connected_ledger). Records where each financial answer came from; server/verification.ts verificationSummary derives the document-verified % over the scored FIN+REV questions (document/ledger = verified, self_reported = not), with default tier thresholds (≥67 document_verified, ≥34 partly_verified) that are a product decision to finalize. Purely descriptive — never reads or writes a score; the DRS stays byte-identical. Exposed as the 'verification-summary' dev function. UI: a VerificationCard on the engagement view (per-input source selector + tier badge + bar) and a "% verified" chip by the DRS dial. The connected_ledger source is a manual stand-in until the QuickBooks/Xero OAuth connect + auto-ingestion land (next Phase 1 slice; needs external OAuth infra). RLS: advisor firm-scoped CRUD, owner reads own company's rows; rls-test +3 (35/35). tests/verification.test.ts (+3) locks the tier thresholds and the verified-count logic.
- 2026-07-13 | Phase 3 (start): owner portal — login infrastructure, QB/Xero connection, education, and read access to the advisor's work | Owners already had RLS read access to their company's completed assessments, results, gaps, tasks, milestones, owner reports, branding, provenance, and world-readable content_modules; this adds the owner-facing surface. Role-aware routing: RequireOwner guard + a branded OwnerShell; RequireAdvisor now redirects owners to /portal. Pages: /portal (readiness home — DRS/ORI/verified%, plain-language tier, top gaps, workspace links), /portal/plan (read-only roadmap tasks + milestones), /portal/learn (content_modules, ones tied to open gaps via gap_content_map flagged "recommended"), /portal/documents (owner_report reports: read inline + download via render-owner-pdf), /portal/connect (QuickBooks/Xero). New migration ledger_connections (provider/status + org name), the connection-state model the real OAuth handshake would write and the verification layer reads; unusually, owners get write access scoped to their own company (they control their books connection). The OAuth handshake itself is external — the connect flow records the connection it would create (manual stand-in), same posture as Phase 1's connected_ledger provenance. RLS: advisor firm CRUD + owner own-company CRUD; rls-test +3 (38/38: advisor/owner ledger isolation, owner can connect own). The portal is branded as the advisor's firm (BrandingProvider), consistent with the reports.
- 2026-07-13 | Learn content unified into the Advisory Library (new item_type 'education') | Migration 20260713000300 (ALTER TYPE advisory_item_type ADD VALUE 'education'). Education pieces now live in advisory_library_items and fire off the same score triggers as buyer questions / initiatives / risk flags — an education guide is "recommended for you" when its governing sub-score/dimension is at or below its score_trigger. Advisors author and filter them in /library (Education added to the type dropdown + filter); owners read them in /portal/learn. server/advisory.ts educationModules(engagementId) returns all education items visible to the firm (system + firm) with a recommended flag computed from the live scores; fireAdvisoryItems now excludes item_type='education' so the advisor buyer-lens is unaffected. Exposed as the 'education-modules' dev function; the owner Learn page moved off content_modules/gap_content_map onto it (useEducation removed). Seed: 7 curated system education pieces in advisory-library.csv (untriggered general guides + ones tied to OPS-HOURS/CUS-TOP1/FIN-ADDBACK/OPS-SOP/REV-RECUR). tests/advisory.test.ts +2 (education firing + advisor-firing exclusion); 64 tests green.
- 2026-07-13 | QuickBooks/Xero ledger sync fills financial assessment answers | server/ledger.ts isolates the (external) accounting API behind pullLedgerFinancials; syncLedgerToAssessment fills the ledger-derivable questions (REV-ANNUAL, REV-TOP5-SHARES, REV-RECUR-PCT, FIN-RECON, FIN-BASIS, FIN-STATEMENTS — 6 of the 10 financial inputs; contract terms, NRR, and add-back judgment stay manual) into an in-progress assessment's answers and marks each connected_ledger provenance, lifting the verification %. The dev adapter derives deterministic, internally consistent figures from the company's books (most-recent answered figures where present, else stable defaults) so the flow works without live OAuth; swapping in the real Intuit/Xero API is confined to pullLedgerFinancials. Exposed as the 'sync-ledger' dev function (advisor-triggered — owner RLS can't see the in-progress assessment). Intake: an "Import from QuickBooks" banner (shown when the company has a connected ledger), a "✓ from QuickBooks" badge on filled questions (via useAnswerProvenance), and provenance auto-downgrades to self_reported when a synced figure is hand-edited (saveStep deletes the provenance row for any changed ledger/document answer) so the verified % stays honest. Nothing is forced — non-derivable questions and manual override remain fully available. tests/ledger.test.ts (+4): fills+provenance+verification, idempotent, refuses without a connection or on a completed assessment. 68 tests green.
- 2026-07-13 | Phase 2 valuation methodology (Matthew's decisions) | (1) Method: market multiple of adjusted (recast) EBITDA. (2) Multiple: industry×size base, scaled by DRS readiness and range-width by verification tier. (3) Net-to-owner: simple configurable model EV − debt − transaction costs − taxes. (4) Seed proposed starter numbers, versioned for later refinement. Starters (illustrative, editable via valuation_rules_version): multiple table field_services 3.5/4.5/5.0, manufacturing 4.0/5.0/5.5, distribution 3.5/4.5/5.0, healthcare 4.5/5.5/6.5, software 5.0/7.0/9.0, default 3.5/4.5/5.0 (size bands <1M / 1-3M / 3-5M adj EBITDA; >5M = 3-5M); readiness adj by DRS tier 1.15/1.0/0.85/0.70/0.55; verification range width ±10/20/30%; transaction cost 8% of EV; blended tax 28% of gain; target DRS 85 for the value-creation gap. Deterministic: valuation_rules compute the number, AI only narrates. Value gaps: value-creation (potential EV at DRS 85 − current) and wealth (owner target − net proceeds).
- 2026-07-13 | Ledger connections over real OAuth (QuickBooks/Xero) + the outcome-calibration moat | Two threads. (A) Connection workflow fixed to a real OAuth 2.0 shape with a dev fallback: migration 20260713000600 adds a service-only ledger_credentials table (tokens, NO grant to authenticated — a client JWT can never read a token, RLS or not), a ledger_oauth_states pending table (CSRF + return path), and realm_id/disconnected_at on ledger_connections. server/ledger-oauth.ts exposes begin → complete → disconnect: begin persists a state and returns the provider authorize URL (env-driven; providerConfig reads QUICKBOOKS_*/XERO_*/LEDGER_OAUTH_REDIRECT_URI), complete exchanges the code for tokens (or synthesizes them in dev when no app is configured) and upserts the connection, disconnect revokes best-effort and flips status to 'disconnected' (row kept for history, tokens wiped). Both surfaces now drive the same three functions via a shared useLedgerActions hook, so owner (OwnerConnectPage) and advisor (AccountingCard) never diverge; disconnect flips status instead of deleting, and the UI gates on status==='connected'. New /ledger/callback route (RequireAuth) completes the real handshake. Dev functions ledger-connect-begin/complete/disconnect are company-scoped (authorized via companies RLS; complete resolves the company from the opaque state). rls-test +3 (44/44: tokens quarantined from both roles). tests/ledger-oauth.test.ts (+6) locks the dev lifecycle. (B) Moat infrastructure (docs/09-moats.md): migration 20260713000700 adds deal_outcomes — one advisor-reported record per closed/broken engagement tying the PREDICTION (DRS/ORI/verified%/predicted EV range, snapshotted at record time from the latest completed assessment + valuation engine) to REALITY (final EV, multiple, EBITDA, buyer type, structure, retrade, days on market, buyer-flagged risks). Reality is fact never inferred; the snapshot freezes what we predicted; calibration reads these rows and never writes a score (a recalibration ships as a new rubric/valuation version). server/outcomes.ts: recordDealOutcome (idempotent per engagement) + firmCalibration (predicted-vs-actual: within-range hit rate, EV variance, avg multiple, retrade rate). DealOutcomeCard on the engagement view captures it; CalibrationPanel on the portfolio dashboard reports it. RLS advisor-firm-only (the firm's internal asset, not owner-portal content). tests/outcomes.test.ts (+3) locks the snapshot + calibration math against the valuation engine. 83 tests green.
- 2026-07-13 | Going-live topology: Vercel (frontend) + Supabase (data/auth) + Render (compute) | Added vercel.json (framework vite, build `npm run build`, output dist, SPA rewrite → index.html so client-router deep links don't 404) and docs/12-vercel-supabase-setup.md, a click-by-click companion to the docs/11 runbook for the specific Vercel+Supabase+GitHub case. Key point made explicit: the app is three runtime pieces, not two — the compute service (server/http.ts, /functions/v1/*, PDF via Chromium in server/Dockerfile) is a long-lived container and belongs on Render/Railway/Fly, NOT Vercel serverless; deploy order Supabase→Render→Vercel then a CORS/redeploy pass. Env split documented (VITE_* on Vercel baked at build time; DATABASE_URL/FUNCTIONS_JWT_SECRET/FUNCTIONS_ALLOWED_ORIGIN/ANTHROPIC_API_KEY on Render). Flagged the JWT signing-key caveat (HS256 path won't validate asymmetric-signed tokens) as a pre-launch follow-up. No code/schema/scoring change — config + docs only.
- 2026-07-14 | Compute service verifies both HS256 and asymmetric (JWKS) Supabase tokens | server/auth-jwt.ts (new, using jose) replaces the hand-rolled HS256 check in server/http.ts. makeVerifyToken inspects each token's `alg` header and routes it: HS256 → the legacy shared secret (FUNCTIONS_JWT_SECRET), ES256/RS256/EdDSA → the project JWKS at <SUPABASE_URL>/auth/v1/.well-known/jwks.json (createRemoteJWKSet, fetched+cached). Supporting both — not swapping — keeps a project working mid rotation; the token's alg picks the path. Startup now requires at least one of FUNCTIONS_JWT_SECRET / SUPABASE_URL (refuses to boot otherwise). jwtVerify enforces signature+exp; we additionally require a string `sub`. Verification is isolated behind a test seam (config.jwks) so tests/auth-jwt.test.ts (+8, pure crypto, no DB) exercises the asymmetric path with a local JWKS and covers wrong-key/expired/no-sub/malformed/mixed-mode rejection. Docs updated: docs/11 §2a, docs/12 env tables, server/Dockerfile env comment. No scoring/schema change; the DRS is untouched. NOTE: the reported client login error "The string did not match the expected pattern" is a browser-side DOMException in supabase-js's fetch/WebCrypto path (not this service and not our code) — almost certainly a Vercel env-var misconfig (unset/whitespace VITE_SUPABASE_URL/KEY, needs redeploy) or a stale localStorage session; diagnosis pending the browser stack trace.
- 2026-07-14 | Trim VITE_SUPABASE_* / VITE_FUNCTIONS_URL env values in src/lib/supabase.ts | Mitigates the reported Safari/iOS login failure "The string did not match the expected pattern": a trailing newline/space pasted into Vercel's env-var UI rides into the request URL and apikey header, and WebKit rejects it when building the request (Chromium is more lenient, so it only showed on iOS). A URL/Supabase key never has meaningful surrounding whitespace, so trimming is safe. isDevStack now keys off the trimmed URL too, so a whitespace-only value doesn't read as "configured". Config-hygiene only; no behavior change for correct values.
- 2026-07-14 | Bridge the Supabase↔Vercel integration's public vars into VITE_ names + render.yaml blueprint | (A) vite.config.ts bridges NEXT_PUBLIC_SUPABASE_URL/SUPABASE_URL → VITE_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY/SUPABASE_ANON_KEY/PUBLISHABLE variants → VITE_SUPABASE_ANON_KEY at build time (only if the VITE_ name isn't already set). The native integration injects credentials under Next.js/generic names, but Vite only exposes VITE_-prefixed vars to the client, so the integration "connected" yet the frontend saw nothing and login broke; the bridge makes it work with zero hand-added frontend env vars. Strictly the two PUBLIC values — the service-role key and JWT secret are never bridged into the browser bundle. Verified: a build with only NEXT_PUBLIC_SUPABASE_URL set bakes that URL into dist. VITE_FUNCTIONS_URL still must be set by hand (no integration can know the Render URL). (B) render.yaml (Render Blueprint) makes the compute service one-click deployable from the repo: docker runtime, server/Dockerfile, /health check, plan standard (2GB — headless Chromium PDF wants the memory; starter can OOM), and the env vars as sync:false prompts (DATABASE_URL, SUPABASE_URL/FUNCTIONS_JWT_SECRET, FUNCTIONS_ALLOWED_ORIGIN, ANTHROPIC_API_KEY). Docs/12 updated with both. Config/docs only; no scoring/schema change.
- 2026-07-15 | Advisor UX cleanup: engagement command view reorganized for hierarchy + a shared Collapsible primitive | The engagement page was a flat wall of ~12 cards with administrative plumbing (owner access, accounting connect, financial verification, deal outcome) rendered *before* the actual readiness picture, and four sub-tool links crowding the header. Reordered so the advisor sees the decision-making story first — current-readiness snapshot + open gaps, then the exit-pace trajectory, score drivers, and compare — moved the four sub-tool links into a single labeled "Workspace" toolbar row, and folded the setup/admin cards into a new default-collapsed `Collapsible` (native `<details>`, keyboard-accessible, token-styled; src/components/ui/Collapsible.tsx). Dropped the redundant Roadmap empty-state block (the Roadmap link now lives in the Workspace toolbar). Presentation-only: no data, scoring, or product behavior changed; verified live in the demo stack (light + dark) that snapshot/gaps/trajectory/drivers/compare render first and the admin cards live behind the collapsed section.
- 2026-07-15 | Consolidate advisor UI into shared patterns: EngagementNav + SectionCard | Two reusable primitives replace hand-rolled markup so the workspace reads as one system. (1) `EngagementNav` (src/components/ui/EngagementNav.tsx) — a NavLink tab bar (Overview · Roadmap · Valuation · Buyer lens · Delta report) rendered on all five engagement-scoped pages, so the tools are one navigable group instead of dead-ends; replaces the one-off "Workspace" toolbar on the overview and the ad-hoc "← Back to engagement" link on the roadmap, and gives Valuation/Buyer lens/Delta sibling navigation they previously lacked. Active tab is route-driven (no `active` prop). Marked no-print on the Delta report so it stays out of the branded artifact. (2) `SectionCard` (src/components/ui/SectionCard.tsx) — a Card with the standard header (uppercase label + optional muted subtitle + optional right-aligned action) that the app repeated ~15× as `<Card><span className="stat-block-label">…</span><p className="muted">…`; applied to the engagement snapshot/gaps/driver cards, the valuation recast/assumptions cards, and the roadmap by-responsible-party card. Removed the now-dead `.eng-snapshot-head`/`.eng-workspace` CSS. Presentation only — no data, scoring, routing targets, or product behavior changed; verified live (overview/valuation/roadmap) that the shared tab bar highlights the right tab and card headers are consistent.
- 2026-07-15 | Typography: self-host Inter + Inter Tight (variable) and unify the numeric hierarchy | The font tokens named 'Inter'/'Inter Tight' but no face was ever loaded, so the app rendered in system-ui everywhere — the intended typeface never appeared. Added @fontsource-variable/inter + inter-tight (weight axis, imported in main.tsx); fonts are bundled at build time so there is still NO runtime network dependency (the original offline/CI-safe intent is preserved — this replaces a system fallback with the real face, it does not add a CDN). Base type refined: font-smoothing, text-rendering, font-feature-settings 'calt', 15px/1.5 body, tighter heading tracking (-0.018em) + line-height 1.2. Every prominent number now shares one register — the display face (Inter Tight), weight 800, -0.02em, tabular — across ScoreDial, stat tiles, results hero tiles, workbench scoreboard, divergence meter, and the valuation EV / value-gap / net-to-owner figures; removed the stray undefined var(--serif) (silently Georgia) on the valuation and owner-value numbers so nothing falls back to an off-brand serif. PDF renderer keeps its system stack (server-side Chromium, separate concern). tokens.ts FONT constant updated to match.
- 2026-07-15 | Spacing: stop paired cards stretching into dead space | Added .eng-grid-top (align-items:start) for card rows whose two cards hold unrelated content of different lengths — the engagement readiness snapshot (short) beside open-gaps (tall), and the valuation EBITDA-recast beside assumptions/net-to-owner — so each card takes its natural height instead of the shorter one stretching with an empty bottom (which read as broken). Results summary tiles (.score-tile) now center their content vertically so the shorter Owner-readiness / reading-together tiles sit balanced in the equal-height row rather than top-aligned with empty space below. Presentation only; no scoring/behavior change.
- 2026-07-15 | Remove the prediction-calibration UI + push secondary content behind disclosures | (1) Removed the outcome-calibration surfaces per product direction: deleted the portfolio CalibrationPanel and the engagement DealOutcomeCard (component files removed; DashboardPage/EngagementPage no longer render them). Backend (server/outcomes.ts, deal_outcomes table, migrations, tests) is left dormant — no schema/scoring change, fully reversible — but the advisor no longer sees any prediction-vs-actual UI. (2) Usability: the default advisor views now show only what an untrained advisor needs, with analytical/power content one click away behind labeled disclosures. Engagement overview keeps the readiness snapshot + open gaps + exit-pace chart visible and folds "Score detail" (drivers + business-vs-owner), "Compare two assessments" (ComparePanel gained an `embedded` prop so it renders inside the Collapsible without its own heading/Card), and "Engagement setup & admin" into default-collapsed sections. Results page folds "The owner's side" (ORI detail) behind a disclosure. Roadmap folds "By responsible party" behind a disclosure. Presentation only; no data/scoring/routing change; verified live (dashboard, overview default + expanded, in light).
- 2026-07-15 | Fix PDF export in local dev + cross-platform Chromium resolution; cap the scenario-workbench board to the viewport | (1) PDF: resolveChromium (server/pdf.ts) only searched Linux cache/system paths (/opt/pw-browsers, /usr/bin/chromium…), so `npm run dev` on a Mac/Windows workstation — where playwright-core ships no browser and a local `npx playwright install` caches under ~/Library/Caches/ms-playwright (macOS), ~/.cache/ms-playwright (Linux), or %USERPROFILE%\AppData\Local\ms-playwright (Windows) — found nothing and the download surfaced "could not launch Chromium". Now scans all per-OS Playwright caches (via findChromiumInCache, which understands chrome-linux/chrome-mac/chrome-win layouts) and common Google Chrome / Chromium / Edge install paths on macOS + Windows; launch error now points devs at `npx playwright install chromium`. tests/pdf.test.ts +3 (synthesized cache layouts). (2) invokeFunctionBlob (src/lib/supabase.ts) now rejects a 200 that isn't a PDF/octet-stream — a misconfigured VITE_FUNCTIONS_URL pointing at the SPA host returned index.html with 200 and it downloaded as a broken .pdf silently; now a clear error. (3) Workbench: .wb-board was position:sticky with no height cap, so a board taller than the viewport had its lower half clipped off-screen on scroll; capped to max-height:calc(100vh-2rem) with internal overflow-y (reverting to static/no-cap under the 900px single-column breakpoint), and tightened the hero number/padding so it fits without scrolling in the common case. Presentation + dev-tooling only; no scoring/schema/product-behavior change.
- 2026-07-16 | Beta Requirement 1 (data-rights capture): immutable engagement agreements + consent gate before any assessment data | New migration adds agreement_versions (firm-scoped, immutable full text — no UPDATE/DELETE grant; new version = new row) and engagement_agreements (per-engagement acceptance, unique per engagement, referencing the exact version, with three consent booleans: benchmarking, anonymized_aggregation, outcome_tracking). assessments gains agreement_version_id. A BEFORE-INSERT trigger on assessments (security definer) blocks any assessment for an engagement without an acceptance and stamps the version in force — the DB-hard guarantee behind acceptance criterion 1 ("no assessment data before acceptance"), firing for every writer incl. service_role. Enforcement is at the assessment level (not a deferrable engagement constraint) to keep autocommit seeds/tests working with low blast radius; the sanctioned app path, the create-engagement function (server/agreements.ts, wired into functions.ts authorize+dispatch, userId threaded into dispatch), still inserts engagement + acceptance atomically in one transaction so app engagements never exist without acceptance. Added the reviewer role (app_role enum; policies land with Requirement 3). Frontend: ClientsPage "Start engagement" now opens a blocking agreement+consent modal (reuses ConfirmDialog) that calls create-engagement; shows a clear message when the firm has no active agreement version. admin CLI gains create-agreement-version (+ reviewer role accepted); seed-demo/seed-portfolio create a firm agreement version + acceptance per engagement; a migration backfill gives pre-existing engagements a retired placeholder acceptance (consent flags false, honestly recording none was captured) and stamps existing assessments. rls-test +3 (agreement_versions/engagement_agreements firm isolation + the gate), 48/48; new functions.test cases prove create-engagement atomicity + 404 on a foreign company; shared tests/helpers.acceptAgreement keeps DB-backed tests green (102/102). No scoring/rubric change — consent/agreement layers never touch a score (rule 2).
- 2026-07-16 | Beta Requirement 3 (document intake + manual review): pluggable parser/storage seams, verified-fact pipeline | New migration adds documents (upload→scan→classify→extract→in_review→verified pipeline status), document_blobs (beta byte store — bytea; R5 swaps to Supabase Storage + encryption + signed URLs behind the StorageAdapter seam), document_fields (per-datapoint verification_status unverified|extracted|verified, value/confidence, optional question link), and field_corrections (parser-accuracy log). Non-negotiable honored: NO parser vendor is hard-coded — extraction goes through ParserAdapter (server/documents/parser.ts) with a ManualParserAdapter default (extracts nothing; the complete manual path) and resolveParser() by EB_PARSER; reducto/llamaparse are recognized-but-unimplemented (throw, not silent-degrade). Storage is behind StorageAdapter (DbBlobStorage now; env seam for R5). Functions accept JSON only, so uploads are base64 in the body (15MB cap) via upload-document; get-document returns a new binary FunctionResult kind (mime + inline disposition, handled in http.ts + dev-server, served with invokeFunctionRawBlob so any doc MIME works — distinct from the PDF helper); list-review-queue, get-document-detail, submit-document-review round out the router (DOC_FNS authorize branch: staff = advisor|reviewer, firm-resolved from profile, engagement/document visibility under RLS). The reviewer role (added in R1) gets its first table policies here (staff_firm_all); owners upload+read their own company's docs. Frontend: engagement Documents tab (upload per category → review queue), /review queue + /review/:documentId detail (source viewer via iframe/img + editable extracted fields → confirm/correct/add → Mark verified; corrections logged), RequireStaff guard + reviewer→/review redirect, Review nav link. Documents/fields never write to a score (rule 2). Verified on fresh DB: migration + seeds load, rls-test 51/51 (+3 document isolation + gate), vitest 108/108 (new documents.test proves upload→verified manual path, reviewer access, binary fetch, correction logging, firm isolation, non-staff 403), tsc + build clean.
- 2026-07-16 | Beta Requirement 5 (security baseline): encryption at rest, signed document URLs, access audit log, MFA, security one-pager | (1) Encryption at rest: document bytes are AES-256-GCM encrypted before storage (server/documents/crypto.ts, key from EB_DOCUMENT_KEY with a documented weak dev default); document_blobs.enc_algo tags the envelope so legacy plaintext rows still read. (2) Signed URLs: source documents are served only via short-expiry HMAC tokens (server/documents/signed-url.ts, default 5-min) through a new unauthenticated GET /documents/download route (server/http.ts + dev emulator), verified before serving; the review viewer uses sign-document-url + documentDownloadUrl instead of a POST blob, so an <iframe> loads it directly. (3) Audit log: new data_access_log (append-only, service-written, advisor-readable) records every document read/download/detail via a best-effort logAccess helper (server/audit.ts) wired into the document functions + the download route. (4) MFA: required for advisor/admin via Supabase AAL — src/lib/mfa.ts (enroll/verify TOTP, getMfaState) + a /security enrollment page + RequireAdvisor gate that redirects un-enrolled/unverified staff to /security; bypassed on the dev stack (no MFA endpoint) and fails-open on transient API error to avoid lockout. (5) docs/13-security-summary.md one-pager (storage, encryption, access controls, retention, subprocessors) + an in-app summary on /security. Storage stays DB-backed behind the StorageAdapter seam (object-storage swap is a later infra step); virus scan remains a recorded stub. Tests: crypto round-trip + tamper, signed-URL sign/verify/expiry (pure), audit-log-written + encrypted-at-rest (DB); rls-test +data_access_log isolation. No scoring change.
- 2026-07-16 | Beta Requirement 6 (usage instrumentation): usage_events + track() across the advisor journey | New append-only usage_events table (firm_id, actor, engagement_id, event_type/name, properties jsonb, session_id, occurred_at) — SQL-queryable, no third-party analytics; RLS lets any authenticated user insert events for their own firm and advisors/reviewers read their firm's stream. src/lib/analytics.ts `track()` is fire-and-forget (per-tab session id, swallows all errors so instrumentation never breaks the UI). Instrumented the journey sufficient to reconstruct where an advisor stalled (acceptance criterion 5): onboarding engagement_started (ClientsPage), assessment_started + assessment_submitted (IntakePage), results_viewed + report_downloaded (ResultsPage/ReportPage — score delivery + next action), document_uploaded (DocumentsPage), document_verified (ReviewDocumentPage). Test proves events land, are firm-isolated, and a session with assessment_started but no assessment_submitted reconstructs as stalled-in-intake; rls-test +usage_events isolation + own-firm insert. Never writes to a score.
- 2026-07-16 | Repo cleanup: env-keys doc (docs/14), dead-code removal, EB_DOCUMENT_KEY prod warning | Added docs/14-environment-keys.md — every env var, which host it belongs on (Vercel frontend vs compute service vs Supabase), required/optional, and where to obtain it, with a quick matrix. Removed confirmed dead code (zero references): src/lib/supabase.ts invokeFunctionRawBlob (orphaned when R5 switched the doc viewer to signed URLs), tokens.ts FONT const, format.ts fmtDateShort (+ its private DATE_SHORT), server/pdf.ts ownerReportDataFromExplain (+ its now-unused ExplainResult import), and src/lib/queries.ts useDealOutcome/useDealCalibration hooks + their types + qk.dealOutcome/qk.calibration keys (orphaned when the prediction-calibration UI was removed in #29; the dormant server-side outcomes backend is intentionally kept per that decision). Added a production startup warning in server/http.ts when EB_DOCUMENT_KEY is unset (documents would otherwise encrypt with the insecure dev default silently). Aligned scripts/e2e-intake.mjs to EB_CHROMIUM_PATH (was CHROMIUM_PATH), backward-compatible. No behavior change; tsc + build clean, rls-test 54/54, vitest 119/119.
- 2026-07-16 | Sell-side intelligence substrate (foundation slice): document-verified knowledge graph, reconciliation, resumable pipeline, findings container, LLM cost ledger | Scaffolds the service layer that turns scored self-assessment into document-verified sell-side intelligence. Deliberately built INSIDE existing conventions (npm single-package, server/ + shared/ + supabase/migrations, the existing ParserAdapter seam) rather than the prompt's literal pnpm /packages monorepo — the repo already implements much of the prompt's surface (ParserAdapter, a document intake+review pipeline, engagements/documents/document_fields/field_corrections, a deterministic scorer), so a restructure would duplicate working seams and break CI/deploy. Confirmed this fork with Matthew before building. Scope this pass is the FOUNDATION only (migrations + schemas + job runner + ontology container + one pipeline vertical through reconcile, all green on fixtures); later steps are declared but stubbed. (1) Migration 20260716000500 adds net-new tables only — graph_nodes/graph_edges (relational graph, JSONB attributes, temporal valid_from/valid_to), assessment_values (self-reported vs document-verified reconciliation with evidence_fact_id provenance), findings (buy-side patterns in reverse, graph_evidence + AI-draft-then-approve), scores (raw|verified, deterministic engine only), jobs (resumable pipeline state, no Temporal), review_items (generalized queue distinct from the per-field field_corrections log), llm_calls (per-call cost ledger) — every table firm_id + staff_firm_all RLS matching the documents layer; rls-test +7 (61 total). document_fields is REUSED as the extracted-facts substrate, extended additively with node_id/source_page/source_span/extraction_run_id (node_id + evidence_fact_id are ON DELETE SET NULL so idempotent rebuilds don't trip FKs). (2) Ontology lives in DATA (seed/ontology/business-ontology.json) behind a registry/loader (server/ontology/registry.ts) that validates node/edge types + attributes — the container the prior repo's ontology ports into, no ontology logic in code. (3) Job runner (server/pipeline/) drives a job through intake→parse→extract→populate_graph→reconcile (implemented, idempotent, checkpointed) and parks at score→match_findings→assemble→deliver (declared, throw NotImplementedError) for later slices; parse reuses resolveParser with a new FixtureParserAdapter (EB_PARSER=fixture) that returns fixture JSON — the prompt's "stub adapter that returns fixtures". reconcile auto-resolves confident agreements and queues conflicts/low-confidence to review_items; engagementMetrics exposes the automation-ratio KPI. (4) Single Claude wrapper (server/llm/client.ts) with retry/timeout/injectable transport + cost logging to llm_calls, a versioned prompt registry (server/llm/prompts.ts, no inline prompt strings), and an extraction eval harness + CI script (server/llm/evals/) over golden fixtures. (5) Findings engine (server/findings/patterns.ts): pattern interface + registry + the customer_concentration reference pattern + a narrative guard that rejects any drafted number absent from the graph evidence (numbers come from the graph, never the LLM); the other four reference patterns land with the findings slice. zod added (only new dep). No score is ever computed or written here (rule 2); the DRS/ORI engine is untouched and will be wrapped by the score step later. Verified on a fresh DB: migrate + rls-test 61 + seed×2 + seed:demo×2 + vitest 141 (unit: ontology/schemas/llm-retry/guard/eval; DB e2e: pipeline through reconcile, graph build, provenance links, low-confidence + conflict review queueing, automation-ratio KPI, customer_concentration match, idempotent re-run) + tsc -b + vite build all clean.
- 2026-07-16 | Verification tab + review-queue API: the advisor-facing surface for document-verified intelligence | Builds the UX that lets an advisor actually drive the sell-side pipeline, plus the review-queue backbone the prompt specified. (1) Review-queue state machine + API (server/review-queue.ts): pending → in_review → resolved | escalated, with list/claim/resolve/escalate; resolving a conflict or low_confidence item writes the human-confirmed verified_value + resolved_by back to assessment_values and flips any job parked on waiting_review back to pending (unblocks the pipeline); resolving a finding_approval item approves/rejects the finding. Side effects commit atomically in one transaction. reviewMetrics exposes the automation-ratio KPI. (2) Verification orchestration (server/sellside.ts): run-verification runs the pipeline (intake→reconcile) then the finding patterns and returns a summary. match_findings is implemented as a standalone runner (server/findings/run.ts) — persisting a findings row + a finding_approval review item per match — kept out of the pipeline's stubbed match_findings step (which sits below the still-stubbed score step) so verification surfaces findings today. (3) Router wiring (server/functions.ts): five staff-only, firm-scoped functions (run-verification, verification-metrics, claim/resolve/escalate-review-item); engagement actions gate on engagement_id, item actions resolve the engagement from the review item; resolved_by/assigned_to resolve to the caller's profile id (not the auth uid). (4) Frontend: a new engagement "Verification" tab (src/pages/VerificationPage.tsx, route + EngagementNav entry) — the advisor's command center: Run button, automation-ratio + verified/needs-review/findings KPIs, the self-reported-vs-document-verified reconciliation table, buy-side findings with severity, and the review queue with inline confirm (editable verified value), approve/reject, and escalate. Reads go direct via supabase under RLS; mutations via the router. Used .in('status',[…]) not .neq (the dev REST emulator supports in./eq./is.null only). (5) seed-demo attaches a fixture financial document to the demo engagement and runs verification so the tab is populated out of the box (idempotent); dev-demo.sh sets EB_PARSER=fixture so uploaded fixtures extract and "Run verification" works live. Verified in a real browser (Playwright on the dev stack): logged in as the demo advisor, the tab renders the KPIs/reconciliation/finding/queue, and clicking Confirm on the low-confidence item resolved it, dropped it from the queue, and moved Needs review 1→0 / Automated 50%→100% live. Tests: sellside-review.test.ts (+5) drives run-verification → resolve low_confidence (verified_value + resolved_by written) → approve finding → metrics → foreign-advisor 404, through the router. Full suite 146, rls 61, tsc + build clean. No score computed or written (rule 2).
- 2026-07-17 | Beta test (advisor): fix silent cross-rubric portfolio delta + cover it with tests | Beta-testing the advisor portfolio dashboard surfaced a silent failure: the "Δ since prior" column subtracted the latest and prior DRS scores unconditionally, so when the prior assessment was scored under a different rubric_version the advisor saw a real-looking movement number that is meaningless (the two scores are not on the same scale). This directly violates build-plan S10 ("when the prior assessment is on a different rubric_version, the delta column shows the incomparable state distinctly … never blank or zero") and docs/03's cross-version delta rule. Root cause: the delta was computed inline inside the untestable usePortfolio hook (Supabase-client-bound) with no rubric_version check. Fix: extracted the row assembly into a pure, dependency-free module (src/lib/portfolio.ts, buildPortfolioRows) that only differences scores when latest and prior share a rubric_version; a cross-version prior is reported as deltaState:'incomparable' (delta stays null) and a single-assessment engagement as 'none'. usePortfolio now just fetches and delegates; PortfolioRow moved to and is re-exported from that module. DashboardPage renders the incomparable state as a muted "new rubric" chip (with an explanatory title) instead of a bogus number or a bare em dash, so the advisor can tell "no prior" from "not comparable". Added tests/portfolio.test.ts (+8) covering the same-version numeric delta, one-decimal rounding, the cross-version incomparable guard (the regression), single-assessment 'none', latest/prior selection by sequence_number regardless of input order, open-gap counting, and the missing-company / no-assessment shells. Presentation + pure-logic only — no scoring, schema, or routing change (rule 2 untouched). Full suite 159, tsc -b + vite build clean.
- 2026-07-17 | Buyer-expectations & vendor-DD analysis (docs/15) — proposal only, no rubric/engine change | Analyzed two real diligence artifacts (a live buy-side DD request list for Majic Stairs, a LMM stair/attic-lift manufacturer, and an RIA vendor DD questionnaire for tech vendors) and mapped them against the current DRS rubric + security posture. Central finding: the DRS measures operating quality well (FIN/REV/OPS/CUS/MGT/GRW all validated by the buyer list) but is nearly silent on the transferability/legal-hygiene items that actually retrade or kill LMM deals — three of the buyer's seven sections (Product & IP, Compliance, Legal) sit outside our measurement surface, and they are exactly where this real deal accumulated risk (IP in a founder's name not the company, no written customer/supplier contracts, a settled fraudulent-PPP disclosure, no buy-sell among three equal owners, single-source foreign motor supply). Proposed three work streams for Matthew to decide: (A) new rubric_version extending FIN/OPS with supplier/channel concentration + sales-tax nexus + inventory controls, and adding a Legal & Transferability layer (IP assignment, contract documentation, litigation/regulatory disclosure, insurance, licenses, product liability) — with an open design decision on scored-dimension vs. hard gating-flag (recommend hybrid so a clean operating score can't mask unassigned IP / undisclosed litigation); (B) a deterministic, no-LLM Data Room Readiness surface that ships the buyer's request list back to owners as their pre-built diligence checklist, reusing the R3 documents pipeline and sharing one taxonomy with the gaps; (C) completing ExitBlueprint's own answer to the RIA vendor packet (dogfood) — extends docs/13, closes named gaps (SOC 2 path, BCP/DR, sub-processor register, ≤30-day breach-notification + orderly-termination language, CISO/background-checks/pen-test cadence), and doubles as services/SaaS-segment content. Appendices A/B line-item the buyer list → DRS coverage and the vendor packet → our posture. No score, rubric row, seed CSV, or engine behavior changed (rules 1–3 untouched); §5 lists five open product decisions reserved for Matthew.
- 2026-07-17 | Decisions on docs/15 (buyer/vendor DD) + Data Room Readiness slice (work stream B) | Resolved the five open questions with Matthew: (1) transferability modeled as a HYBRID — score the gradable items, hard-flag the binary deal-killers; (2) rubric scope SPLIT — legal/IP/transferability universal, industry signals in rubric_version overlays; (3) tier gating HARD CAP — unassigned IP / undisclosed litigation / open regulatory action forbid Institutional Grade outright; (4) data-room and gap taxonomies share ONE source of truth; (5) build work stream B first. Then built B: a canonical, versioned Data Room Readiness template (7 sections, 37 items) generalized from the real Majic Stairs buy-side list and seeded as global methodology like the rubric (seed/data-room-sections.csv, seed/data-room-items.csv, parsed+validated via shared/rubric-seed.ts validateDataRoom, loaded idempotently by scripts/seed.ts), plus a per-engagement readiness state per item (not_started|in_progress|ready|gap|not_applicable) in a new firm-scoped engagement_data_room_items table with a link to an R3 document. Migration 20260717000100_data_room.sql: template tables are methodology-read (global), the tenant table gets staff_firm_all + owner_own_company RLS mirroring the documents pipeline. server/data-room.ts (listDataRoom/setDataRoomItem) wired into functions.ts as list-data-room/set-data-room-item, authorized via the generic engagement path (staff by firm, owner by company); firm_id is resolved from the engagement, never the body, and a linked document must belong to the engagement. Items that map to a scored gap carry that gap's code (decision 4) — CUST_CONC, CONTRACT_GAP, ADDBACK_RISK, RECON_GAP, STMT_INCOMPLETE, SOP_GAP, OWNER_DEP, MGMT_DEPTH, NONCOMPETE_GAP; items whose gaps arrive with work stream A (IP_UNASSIGNED, TAX_NEXUS_EXPOSURE, SUPPLIER_SINGLE_SOURCE, etc.) carry no code yet by design. Frontend: an engagement "Data room" tab (src/pages/DataRoomPage.tsx + route + EngagementNav entry) showing sections in buyer-list order, a per-item state selector, the buyer rationale ("why buyers ask"), a gap-code tag, and a readiness-percent summary (ready ÷ in-scope). Deterministic, no LLM, never writes a score (rule 2 untouched). seed-demo populates 8 demo states so the tab renders a realistic mix. Verified on a fresh DB in CI order: migrate + test:rls 67 (+6 data-room isolation) + seed×2 idempotent + seed:demo×2 + vitest 167 (+8, tests/data-room.test.ts drives the router e2e incl. invalid-state/unknown-item/foreign-document/owner-access/foreign-advisor) + eval + build all green, and the tab driven live in a browser against the demo tenant (37 items, 6% readiness, gap tags visible, no page errors).
- 2026-07-17 | Data Room follow-ups (generalize template + per-item document tagging) & vendor-DD security hardening (work stream C start) | (B follow-ups) Generalized the data-room template to all-business: every item is now applies_to='all' and product/manufacturing-specific wording (SKUs/BOMs/rated capacity/installers) was reworded to product-OR-service language (industry specifics belong in a future overlay per decision 2); section names generalized (PIP "Offering & Intellectual Property", OPS "Operations & Suppliers"). Still 7 sections / 37 items. Added per-item document tagging: a new attach-data-room-document router function (server/data-room.ts attachDataRoomDocument) uploads a file through the existing R3 ingest pipeline with a data_room:<item_code> category so parse/extract knows which diligence item it answers, links it to engagement_data_room_items.document_id, and advances an untouched item to in_progress (leaves explicit states). listDataRoom + setDataRoomItem now join documents to return document_filename/document_status; the Data room tab shows each item's linked document + status and an Upload/Replace control (base64 upload, same pattern as DocumentsPage). Authorized via the generic engagement path (staff by firm, owner by company); firm resolved from the engagement. (C start) Security to the vendor-DD (LWG packet) standard: new docs/16-vendor-security-dd.md maps every packet section to our actual posture, honestly marked implemented(✅)/documented(📄)/planned(🟡) — no control claimed that isn't true (SOC 2, pen test, background-check policy, ≤30-day breach-notification clause, CI vuln scanning, DR-test cadence, self-serve export+purge are the tracked 🟡 items). Implemented one concrete control the packet explicitly asks for — automatic shutdown of inactive sessions: a 30-minute idle-session timeout in src/lib/auth.tsx (armed only while signed in; pointer/key/scroll/visibility activity resets it). Added a canonical sub-processor register (seed/subprocessors.csv: Supabase, Vercel, Anthropic) and expanded retention/orderly-termination/BCP-DR + idle-timeout in docs/13 and the /security page (accurate sub-processors, docs/16 pointer). No score/rubric/engine change (rules 1-3 untouched); the data room stays deterministic and LLM-free. Verified on a fresh DB in CI order: migrate + test:rls 67 + seed×2 idempotent + seed:demo×2 + vitest 169 (+2 attach tests: staff+owner upload, category tagging, list join) + eval + build all green, and the upload flow driven live in a browser (item Not started → In progress with the linked document shown, no page errors).
- 2026-07-17 | DRS-2.0 second-pass rubric drafted (docs/DRS-2.0-rubric-draft.xlsx) — proposal, not applied | Reviewed the v1 rubric against how due diligence is actually run in the lower middle market (web research: QoE is the largest DD workstream with a 10–30% seller-vs-adjusted-EBITDA gap; customer concentration is the #1 deal-killer; NWC pegs are universal; sales-tax nexus, IP assignment, and environmental/CERCLA are common retrade/kill items) plus the two real DD artifacts (Majic buy-side list, LWG vendor packet). Produced a 9-sheet weighted xlsx (Read Me, DRS Weights v1-vs-v2, Sub-Scores v2, Transferability, Gating Flags, New Gaps, DD Coverage Map, Industry Overlays, Research & Sources). Key v2 moves: (1) new 7th business dimension TRF (Transferability & Legal Readiness, weight 0.10) with 7 sub-scores (IP assignment, contract documentation, corporate/governance, litigation/regulatory, insurance, licenses, environmental) — the hybrid: gradable items scored, binary killers also hard-gated; (2) new signals in existing dimensions — OPS-SUPPLIER (single-source), CUS-CHANNEL (channel concentration), FIN-WORKINGCAP + FIN-TAX, REV-PRICING; (3) de-duplicated customer concentration (removed REV-HHI, kept it in CUS); (4) five hard gating flags (IP_UNASSIGNED, LITIGATION_MATERIAL, REGULATORY_OPEN, ENVIRONMENTAL_CONTAMINATION, TAX_NEXUS_MATERIAL) that cap the tier at Sale Ready per decision 3; (5) rebalanced DRS weights REV .22/FIN .20/OPS .18/CUS .15/MGT .08/GRW .07/TRF .10 (operating dimensions still 0.90). 12 new gap definitions reuse the work-stream-B data-room item codes (one taxonomy, decision 4); industry-specific signals kept in overlays (decision 2). All weight sets validated to sum to 1.00 (per-dimension and effective). No engine/seed/score change — this is a design artifact for Matthew to approve before it becomes a new rubric_version.
- 2026-07-17 | Sell-side work streams: deep-dive + consolidate engagement nav by work stream (docs/17) | Named the five sell-side preparation work streams the product actually serves — Readiness (assess + diagnose: Overview, Buyer lens, assessment/results), Remediation (Roadmap + education), Evidence (Data room + Documents + Verification — the diligence binder), Value (Valuation + Workbench), Deliverables (reports + Delta) — and dived deep on each (the job it answers, its core concept, its surfaces, sequence, and definition of done). Consolidated the feature spread: the engagement navigation moved from eight flat, ungrouped tabs to five labeled work-stream groups (EngagementNav.tsx now renders WORK_STREAMS groups with an uppercase label + nested links; new .eng-nav-group/.eng-nav-group-label/.eng-nav-links CSS with subtle vertical dividers). Biggest win: Data room, Documents, and Verification now read as the one Evidence job instead of three sibling features. docs/17 also maps every current surface (incl. cross-cutting Portfolio, Review queue, Library, Trust & Security) to a work stream and specs the next step (per-work-stream progress on Overview). No routes, scoring, or data change — pure information architecture. Verified: build clean; driven live in a browser (groups render READINESS/REMEDIATION/EVIDENCE/VALUE/DELIVERABLES with all eight tabs nested, active state intact, no errors).
- 2026-07-17 | Model the CFP/CEPA practitioner workflow: three-legs-of-the-stool alignment (docs/18) | Reviewed how CEPAs (EPI Value Acceleration Methodology — Discover/Prepare/Decide gates, three legs of the stool, wealth/value/profit gaps, four intangible capitals, attractiveness-vs-readiness, 90-day sprints) and CFPs (7-step process) actually work, mapped them onto our five sell-side work streams (docs/17), and produced a gap analysis (docs/18). Built the signature gap — the Three Legs of the Stool alignment — as a deterministic, LLM-free panel on the engagement Overview (Readiness work stream): Business leg from DRS/tier, Personal leg from ORI (pulled down by an open TIMELINE_MISMATCH gap and named as the most-overlooked leg), Financial leg from the valuation's already-computed wealth gap (owner_wealth_target − net_proceeds: goal covered / sized gap / not-yet-quantified, reinforced by VALUE_GAP), plus a wobbly-stool verdict and the Value Acceleration gate the engagement is at (Discover/Prepare/Decide). Logic lives in a pure, unit-tested module (src/lib/alignment.ts + tests/alignment.test.ts, 10 cases) rendered by EngagementPage — mirrors the buildPortfolioRows pattern; assembles data the platform already produces, so no engine/scoring/schema/rubric change. Verified: build clean, vitest 179 (+10), and the panel driven live in a browser against the demo (Business On track 72·Sale Ready, Personal Building ORI 63, Financial Not sized → Discover gate, no errors). docs/18 also specs the remaining practitioner gaps in priority order (wealth-gap capture, gate/sprint framing, four-capitals lens, Decide-gate options, profit gap) for Matthew to prioritize.
- 2026-07-17 | docs/18 #1 built: first-class wealth-gap capture (Value screen) + alignment cross-link | The owner's wealth target already fed the valuation's wealth_gap but was buried in advanced inputs, so the Three Legs financial leg read "not sized." Elevated it to a first-class capture prompt on the Valuation headline (segmented on the Value work stream); the alignment financial leg links to Valuation when unsized (summarize on Readiness, capture on Value). seed-demo seeds a recast + $8M wealth target so the demo shows a complete three-legs picture. Presentation + demo-data only; no engine/scoring/schema change. Verified fresh in CI order (rls 67, vitest 179, build) and the capture flow driven live across screens.
- 2026-07-17 | docs/18 #2 & #3 built: 90-day sprints on the roadmap + Four Intangible Capitals lens | Two more practitioner-vocabulary lenses, both pure rollups over existing data (src/lib/practitioner.ts, unit-tested tests/practitioner.test.ts +5). (1) Roadmap open tasks now group into 90-day sprint windows (This sprint / Sprint 2 / Sprint 3 / Later / Unscheduled) — the CEPA Prepare-gate cadence — replacing the flat task list (Remediation work stream); done tasks collapse into a Completed group. (2) The six business dimensions roll up into the Four Intangible Capitals (Human=MGT, Structural=OPS+FIN, Customer=CUS+REV, Social=GRW) on the Overview score-detail view (Readiness work stream) — the CEPA vocabulary as a lens, deliberately not a re-scoring. No engine/scoring/schema change. Verified: build clean, vitest 184 (+5), and both driven live in a browser (roadmap sprints 8/4/1; capitals Human 76/Structural 71/Customer 77/Social 52; no errors). docs/18 now has only gap 7 (Decide-gate options) and gap 4 (profit gap) remaining.
- 2026-07-17 | Roadmap + engagement catch-up controls for engagements already in flight | Advisors onboarding an engagement that began months before it was entered here can now catch the system up to reality, not just start greenfield. (1) Engagement timeline: a new editable "Engagement timeline" card in Engagement setup & admin lets the advisor set the real (past) start date and target exit window (supabase update engagements under advisor RLS) so the trajectory/exit-pace and the 90-day sprints anchor to the actual engagement, not the create date. (2) Per-task due dates: each open remediation task now has an inline date input to set its real due date (previously only a bulk "reschedule from start date" anchor existed). (3) Manual task add: an "Add a task" form on the roadmap captures work already underway or planned outside the generated playbooks (title + responsible party + due date; status todo, then markable done), complementing the gap-generated tasks and personal milestones. All presentation + direct RLS-scoped writes; no engine/scoring/schema change. Verified: build clean, vitest 184, and all three driven live in a browser (13 editable due inputs, add-task 13→14 and visible, start date set to a past date and persisted through reload; no errors).
- 2026-07-18 | Category-B start (docs/20/21): engagement_log — institutional memory (meetings, decisions, rationale) | First build off the strategic reprioritization: capture the advisor's *reasoning*, not just the action the system already records. New engagement_log table (migration 20260718000100) — firm_id, engagement_id, author_id, kind (meeting|decision|rationale|note), occurred_on (backdatable for in-flight engagements), title, detail, and gap_id (nullable, ON DELETE SET NULL) so a rationale attaches to the specific recommendation it explains. Staff-only under RLS (advisor+reviewer firm CRUD; NO owner policy — internal advisory logic, never client-facing); never writes to a score (rule 2). Surfaced as an "Engagement log" collapsible on the engagement Overview: a typed, dated, gap-linked list + add/remove form (direct supabase under RLS, matching the milestones/tasks pattern; useEngagementLog hook + qk.engagementLog). seed-demo seeds 3 entries (kickoff meeting, a prioritization rationale, a decision). Documented in docs/02; docs/21 updated (structured-meeting-outcomes + advisor-rationale B items now BUILT v1; next is wiring entries into graph_edges). Verified fresh in CI order: migrate + test:rls 71 (+4 engagement-log isolation incl. owner-cannot-read) + seed×2 + seed:demo×2 (idempotent) + vitest 184 + build all green, and driven live in a browser (3 seeded entries render by kind, adding a decision → 4, no errors).
- 2026-07-18 | Category-B: connect the knowledge chain — "How the plan connects" (docs/21) | Second Category-B step: connect the institutional record the platform already holds into one view instead of isolated records. buildEngagementKnowledge (src/lib/knowledge.ts, pure + unit-tested) assembles, per gap, the chain gap(risk) → recommendation(mapped playbook) → the advisor's logged reasoning (engagement_log entries tied to that gap) → progress (tasks done/total), severity-ordered, plus a connectedPct (share of logged reasoning tied to a specific recommendation) and the unlinked reasoning. Surfaced as a "How the plan connects" collapsible on the engagement Overview (uses gaps + tasks + log already fetched; mirrors the buildAlignment/buildPortfolio pure-function pattern). Deliberate design choice: the advisory chain is assembled DETERMINISTICALLY over source tables, NOT duplicated into the document-verified graph_nodes/graph_edges (those remain for extracted business facts) — same information, no sync/staleness, no coupling to the extraction ontology. seed-demo now links 2 of 3 log entries to their gaps so the chain shows end-to-end. No engine/scoring/schema change. Verified fresh: build clean, vitest 188 (+4 knowledge), seed:demo idempotent, and driven live in a browser (4 chains, Risk → "Management Depth Program" → "1 rationale", 67% connected, no errors).
- 2026-07-18 | Category-B: comparable engagements — learn from the firm's own book | Third Category-B step (docs/20/21 "relevant historical cases"): surface prior engagements in the SAME firm that resemble the current one, ranked by industry (weight 3), size band (2), and each shared open gap (1). Pure deterministic ranking in shared/comparables.ts (rankComparables, unit-tested tests/comparables.test.ts +5, incl. weighting order, zero-match exclusion, closed-outcome tie-break, limit); server/comparables.ts (engagementComparables) fetches the firm's sibling engagements with latest DRS/tier (active_assessments), outcome status, and open-gap codes and ranks them; wired as the engagement-comparables router function (authorized via the generic engagement path — the current engagement is authorized under RLS and siblings are constrained to its firm_id server-side, never cross-firm; anonymized cross-firm benchmarking stays out of scope). Surfaced as a "Comparable engagements" collapsible on the engagement Overview (company link + tier + outcome + why-similar reasons). No engine/scoring/schema change. Verified: build clean, vitest 193 (+5), and driven live against a 15-engagement portfolio seed (5 comparables ranked by shared gaps, no errors).
- 2026-07-18 | Outcome capture — the calibration substrate (docs/09 moat 1), per "we do need to capture outcomes" | Re-introduced OUTCOME CAPTURE (the advisor-reported facts), distinct from the predicted-vs-actual comparison UI that was deliberately removed earlier — that comparison surface was NOT re-added. A "Deal outcome" collapsible on the engagement Overview records the result at close/break via the existing (dormant) record-deal-outcome function: outcome (closed|broken|withdrawn), close/break date, days on market, final EV, final multiple, EBITDA at close, buyer type, structure, retrade + %, notes, and — the highest-value calibration signal — WHICH open gaps the buyer actually flagged (checkboxes from the engagement's gaps, stored in deal_outcomes.buyer_flagged_risks). recordDealOutcome already snapshots the prediction (predicted DRS/ORI/verified%/EV range from the latest assessment + valuation) at record time, so each row ties a prediction to reality — the FICO moat. The capture also syncs engagement_outcomes.process_status to match. New DealOutcomeCapture component + useDealOutcome read hook; direct record via the router function + supabase upsert under advisor RLS (deal_outcomes + engagement_outcomes both advisor_firm_all). No engine/scoring/schema change (the substrate already existed). Verified: build clean, vitest 193, and driven live in a browser (recorded a closed deal $9.5M/5.1x with a buyer-flagged gap; reload confirmed persistence + "Update outcome" state; no errors).
- 2026-07-18 | UX review + consolidation into core workflows (docs/22): work-stream progress rail | Reviewed the advisor workspace against the five sell-side work streams (docs/17) and strategic IA (docs/19–21). Headline finding (F1): EngagementNav names the arc but the Overview never showed *progress* through it — an advisor had to open each tab to learn where the engagement stood. Built the deferred docs/17 follow-up: a work-stream progress rail atop the engagement Overview. buildWorkstreamProgress (src/lib/workstreams.ts, pure + unit-tested, tests/workstreams.test.ts +9) returns the five streams (Readiness → Remediation → Evidence → Value → Deliverables) each with a state (done|active|todo|blocked — downstream is blocked until a scored assessment exists) and a data-derived headline (DRS, tasks done/total, % verified, EV+gap, report status). WorkstreamRail renders them as a numbered, deep-linking arc; the biggest legibility win is Evidence collapsing its three tabs (data room/documents/verification) into one state. Fed entirely by data the Overview already loads — no new queries, no scoring, no writes (rules 1–2). docs/22 records the full review incl. deferred items F2 (deep-merge the three Evidence tabs into one tabbed surface), F3 (fold assessment-scoped pages into the engagement shell), and a future Portfolio rail (buildWorkstreamProgress is written for reuse). No routes/schema/scoring/AI change. Verified: tsc clean, vitest 111 passed/91 skipped (+9 workstreams), build clean, and driven live in a browser (rail renders 5 chips with correct states — DRS 72 done, 0/13 tasks active, evidence todo, EV modeled·$1.5M gap done, no report todo — no app errors).
- 2026-07-18 | Posture vs vision — database-enforced assessment immutability (docs/23) | Reviewed the vision's (docs/20) load-bearing claims against what the infrastructure actually enforces. Most hold (deterministic scoring is CI-verified vs reference_scorer; multi-tenant RLS is regression-tested; audit trail data_access_log is wired via logAccess). The one gap: "immutable snapshots / defensible evidence / records that withstand institutional diligence" (rule 4) was enforced ONLY by app convention (server/scoring.ts + ledger.ts throw), while the data layer's advisor_firm_all `for all` policy let a client with an advisor JWT UPDATE a finalized drs_score, rewrite its answers, or DELETE the snapshot through PostgREST with no supersede record. Built the guarantee: migration 20260718000200 adds BEFORE UPDATE/DELETE triggers that freeze a completed assessment and its scored children (answers, sub_score_results, dimension_scores). Role-scoped to the untrusted path — the triggers constrain `authenticated`/`anon` (end-user JWT) only; the trusted backend (service_role) and admin/superuser pass through, because corrections are orchestrated server-side as supersede and tenant offboarding/backfills must stay possible. Triggers (not RLS) because they apply to any writer and can express "frozen once completed except via the server path"; scoring is unaffected (INSERTs children while in_progress, flips to completed last). Verified full CI-exact on a fresh DB: test:rls 77 (+6 immutability checks as an authenticated advisor), vitest 202 (DB-integration suites run as superuser, correctly exempt, score→supersede→teardown unaffected), seed/seed:demo ×2 idempotent, eval 8/8, tsc + build clean; plus direct SQL confirming each freeze fires and the supersede bookkeeping update still lands. No scoring/rubric/AI change; app surfaces unchanged. Deferred (recorded): aggregate calibration read, AI institutional reviewer, knowledge-graph materialization.
- 2026-07-18 | Spacing system — a 4px scale + intentional engagement-flow rhythm (advisor UX) | Addressed the "vibe coded" feel: spacing was ~40 hand-picked rem values (0.35, 0.4, 0.6, 0.88, 1.15, 1.3…) with no shared rhythm, and the engagement Overview stacked every block on a uniform 1.5rem gap so nothing read as hierarchy. Introduced a documented spacing scale in styles.css :root (--space-1…9 on a 4px grid) plus semantic aliases (--gap-tight/-block/-section, --pad-card, --pad-page-x), and refactored the shared layout primitives every advisor surface inherits — .page, .ui-card(+lg), .ui-pageheader/-breadcrumb/-sub/-actions, .section-card-head/-sub, .stat-row/.stat-block, .stack-lg, .app-bar-inner/-left/-right, .filter-row/-control, .page-title-row, .section-heading/-sub — to reference the scale (values snapped to the nearest step, preserving density while giving rhythm). Engagement-flow placement: the tab bar (.eng-nav) now reads as an extension of the page header via `.stack-lg > .ui-pageheader + .eng-nav { margin-top: calc(var(--space-2) - var(--gap-block)) }` (header + nav = one "where am I / where can I go" cluster, then a full section break into the working content), and .eng-nav/.ws-rail/.eng-grid spacing moved onto the scale. No JSX/routing/component API change (pure CSS token refactor); no scoring/rubric/AI/schema change (rules 1–6 untouched). Verified: vite build clean, and the shared primitives + engagement chrome rendered live in a browser via the /dev/components gallery and /login (consistent card/stat/section rhythm, no regressions).
- 2026-07-18 | Path to production v2 — adopt Clerk (auth) + Stripe (billing), RLS-preserving (docs/24) | Matthew set direction beyond docs/10's "first paying customer": (1) AUTH moves to Clerk as the identity provider, integrated via Supabase THIRD-PARTY AUTH so the entire RLS model is preserved rather than replaced — Clerk issues the session JWT, Supabase validates it via Clerk's JWKS (the Node service's makeVerifyToken already supports a JWKS URL), and the three security-definer helpers (app.user_role/user_firm_id/user_company_id) change ONLY from reading auth.uid() to auth.jwt()->>'sub'; every RLS policy body is unchanged. profiles.user_id widens uuid→text (Clerk user_… ids), the auth.users FK is dropped, usage_events.actor_user_id widens too, and firms gains clerk_org_id (Clerk Organizations = firms; internal firm_id uuid stays the key). Invites move from invite.ts's direct auth.users insert to Clerk organization invitations + webhook/first-request profile provisioning; MFA moves from Supabase AAL (src/lib/mfa.ts) to a Clerk policy. The 72 RLS checks must pass under Clerk-shaped JWTs — that's the go/no-go gate. (2) BILLING is Stripe, per-firm subscription + seats (advisor firm = Stripe Customer = Clerk Org): new plans/firm_subscriptions/billing_events tables (firm-scoped, staff-read, service-role-write), stripe_customer_id on firms, a server/stripe.ts with checkout + billing-portal + a signature-verified idempotent webhook handler (raw-body), a server-side entitlement gate in the functions.ts authorize path (no active sub ⇒ paid actions refused, own-data reads stay free), seat enforcement at invite, and past_due→grace→read-only dunning. Wrote docs/24 (re-baseline of current state, full gap inventory P0–P2 across auth/billing/ops/security/legal, detailed Clerk + Stripe designs, DoD, phased sequencing ~3–4 weeks, 7 open decisions for Matthew incl. pricing tiers/trial/gated-action list). NO code implemented yet (plan-first, per the chosen deliverable); rules 1–4 and 6 untouched, no scoring/rubric/AI change. CLAUDE.md's "Supabase Auth" stack line + rule-5 wording to be updated when Clerk lands, not before.
- 2026-07-18 | Clerk+Stripe slice 1 — DB foundation (docs/24 A2+B1), RLS gate passed | First build slice off the docs/24 plan and the three approved combos (Clerk 3rd-party auth; per-firm/seat billing with Solo/Practice/Firm tiers; default free-vs-gated line). Landed the external-account-independent core: (A2) migration 20260719000100_clerk_identity — profiles.user_id + data_access_log/usage_events.actor_user_id widen uuid→text (Clerk ids), profiles→auth.users FK dropped, firms.clerk_org_id added, the three app.user_* security-definer helpers switch from auth.uid() to auth.jwt()->>'sub', and own_profile_read (the only direct-auth.uid() policy) is dropped+recreated against the text identity; db/supabase-shim.sql gains auth.jwt() so it runs on plain Postgres/CI; scripts/rls-test.ts mints Clerk-shaped text subjects; a transitional u.id::text=p.user_id cast keeps server/invite.ts (+its test) compiling until the Clerk invite slice replaces that path. (B1) migration 20260719000200_billing — plans (seeded Solo/Practice/Firm with seat+engagement limits and feature sets), firms.stripe_customer_id, firm_subscriptions (one cached row/firm), billing_events (idempotent webhook log); RLS: plans read-all-authenticated, firm_subscriptions staff-read-own-firm, billing_events service-role only. Go/no-go gate PASSED — RLS isolation holds under Clerk identity: 80 checks pass (77 prior + 3 billing) on a fresh DB, and the full CI-exact sequence is green (migrate→rls80→seed×2→seed:demo×2→vitest202→build→eval8/8, tsc clean). No scoring/rubric/AI change (rules 1–4,6 intact); rule 5 preserved by construction (policy bodies unchanged, only the identity source swapped). CLAUDE.md stack line to change when the full Clerk wiring lands (frontend/server JWKS), not at this DB slice. On a new branch per Matthew (keeps the spacing PR #40 clean).
- 2026-07-18 | Beta readiness — entitlements + comp path + flag-gated feature gate (docs/25) | With Clerk/Stripe DB foundation merged, built the billing piece a comped BETA actually needs (not checkout/webhooks): an entitlement system so beta-comped firms get full access with no Stripe, and enforcement machinery that is OFF by default so nothing is gated during beta or in the current app. (1) migration 20260719000300 adds firm_subscriptions.comp (a firm is fully entitled when comp=true, overriding Stripe status). (2) shared/entitlements.ts — pure, unit-tested resolveEntitlement(sub, plan) → {entitled, reason (comp|active|trialing|past_due_grace|none|inactive), plan limits, features} + hasFeature + withinLimit (11 tests); past_due stays entitled (grace; the time-based read-only cutover is deferred to the Stripe dunning slice). (3) server/entitlements.ts — getFirmEntitlement (reads the cached rows as service role) + entitlementGate(name, firmId, db): returns a 402 message only when BILLING_ENFORCED=true AND the action is in GATED_FNS (create-engagement, score-assessment, compute-valuation, generate-roadmap, generate-document, render-owner/delta-pdf, invite-owner — viewing existing data is never gated) AND the firm is not entitled; comped firms always pass (5 tests via a fake pg client). (4) Wired as ONE checkpoint in handleFunctionCall between authorize and dispatch — default-off, so zero behavior change to the running app or a comped beta. (5) Settings → "Billing & access" read-only card reusing the pure resolver client-side (shows Beta access — complimentary / plan / seats). (6) docs/25 beta-launch runbook: the credential/infra checklist (Path A = Supabase Auth now / Clerk later since the identity migration is backward-compatible, Path B = Clerk from day one, plus the GA billing-on steps), separating [you] infra/secrets from [code] already-built. No scoring/rubric/AI change; rule 5 intact. Verified CI-exact on a fresh DB: migrate → rls 80 → seed×2 → seed:demo×2 → vitest 218 (+16 entitlement) → build → eval 8/8, tsc clean.
- 2026-07-18 | Institutional UI — engagement tab bar + workstream rail spacing | Reviewed the engagement chrome against institutional-software conventions (tabs are navigation; a separate band carries workflow state) and fixed the two flaws a reviewer would flag. (1) The tab bar's uneven, broken-looking gaps had a concrete cause: each work-stream group was rendered as a column with a stacked UPPERCASE label (READINESS/REMEDIATION/EVIDENCE/VALUE/DELIVERABLES), so the group's width was set by the label — wider than short tabs like "Roadmap"/"Delta report" — stranding them with large gaps. (2) Those same five labels were ALSO shown in the WorkstreamRail directly below, duplicating the taxonomy within ~100px. Fix: EngagementNav drops the group labels and renders a clean, evenly-spaced single row of tabs, with the five groups expressed only as a subtle vertical divider + breathing room (var(--space-4) around each divider); the WorkstreamRail is now the single home of the labeled READINESS→DELIVERABLES arc. Also snapped the rail chips to the spacing scale (padding var(--space-3), tokenized micro-gaps) so they read as a crisp numbered stepper. Removed the now-unused .eng-nav-links / .eng-nav-group-label classes. Pure frontend + CSS; no routes/scoring/schema/AI change. Verified live in a browser against the seeded demo tenant: the tab row is evenly spaced with group dividers and an accent underline on the active tab, wraps gracefully at narrow widths keeping groups intact, and the rail no longer duplicates the tab labels; tsc + vitest 218 + build clean.
- 2026-07-18 | Institutional UI polish — remove machine-artifact leakage + consistency fixes | System-wide UI review (drove all advisor pages in a browser) surfaced oddities that read as "unfinished"; fixed the systemic + low-risk ones. (1) MACHINE CODES LEAKED TO THE UI: Verification showed raw field keys (annual_revenue, ebitda, customer_concentration) and Data room showed UPPER_SNAKE gap codes (STMT_INCOMPLETE, CUST_CONC, …) as chips. Added two pure, unit-tested helpers to src/lib/format.ts — humanizeKey (snake_case → readable label, with finance-acronym casing: EBITDA/GAAP/SOP/%…) and formatFieldValue (context-aware: money fields → currency, ratio/percent → %, other numbers → thousands separators, text passthrough). Applied across VerificationPage (reconciliation table field + values, findings, review queue); Data room's raw gap_code chip became a uniform "Scored gap" tag (the code moved into the tooltip). (2) UNFORMATTED FINANCIALS: Verification showed 10000000 / 2000000 as bare integers → now $10,000,000 / $2,000,000 (the editable review-queue input stays raw so it round-trips). (3) STAT-TILE HINT INCONSISTENCY: gave every tile in the Portfolio + Verification StatRows a sub-hint so tiles align (was: some hinted, some not). (4) DISCLOSURE INCONSISTENCY: Results' "How this score is built" used a ▸/▾ text marker while the rest of the app uses the Collapsible chevron — converted it to the shared Collapsible (retired the dead .how-it-works CSS). (5) CARD-HEADER INCONSISTENCY: Security used title-case <h3> headings while the app uses SectionCard uppercase micro-labels — converted both Security cards to SectionCard. (6) UNSTYLED FILE INPUT: styled input[type=file]::file-selector-button to match the design (Settings → Logo). (9) DEV-ARTIFACT LEAK: Security referenced internal repo paths (docs/13-…md, docs/16-…md) → replaced with "available on request". Pure frontend + CSS; no routes/scoring/schema/AI change. Deferred to a second pass (layout judgment): Roadmap's empty-left/long-right two-column imbalance and its header control crowding; Results' equal-height summary cards leaving dead space. Verified: tsc + vitest 225 (+7 format) + build clean, and every fix confirmed live in a browser against the seeded demo tenant.
- 2026-07-18 | Institutional UI polish (second pass) — layout-judgment fixes | Completed the deferred items from the UI audit. (7a) ROADMAP two-column imbalance: the Milestones/Remediation-tasks split was a symmetric .eng-grid, so the sparse Milestones side stranded a tall empty column next to the long task list — replaced with an asymmetric .roadmap-cols (Milestones = a compact ~22rem left rail; tasks take the wide column), collapsing to one column under 860px. (7b) RESULTS equal-height dead space: the three summary tiles (dial + two readings) stretched to equal height, floating short content in tall tiles — restructured into .score-summary (dial anchors a fixed-width left column; the two readings stack in .score-summary-side and split the height, so nothing is left with dead space). (8) ROADMAP header crowding: the Start-date + Reschedule control cluster crowded the page title — moved it to a right-aligned .roadmap-toolbar directly above the Gantt (the timeline it drives), which also removed a duplicate "Build roadmap from gaps" button that appeared in both the header and the empty state. (10) SETTINGS title mismatch: the page hosts Billing + Branding but was titled "Firm branding" → retitled "Firm settings" with a subtitle covering both. Confirmed no other card-header offenders remain (#5). Pure frontend + CSS; no routes/scoring/schema/AI change. Verified: tsc + vitest 225 + build clean, and all four fixes confirmed live in a browser against the seeded demo tenant (balanced results tiles, compact milestones rail + readable task cards, uncrowded roadmap header with the reschedule toolbar above the Gantt, "Firm settings" title).
- 2026-07-18 | Report/PDF workflow — polished doc auto-generates, raw Markdown behind an expand bar | Reworked the owner-report and delta-report pages so the advisor lands on a finished, branded document instead of an empty page and a wall of raw Markdown. (1) AUTO-GENERATE: on first visit, if no report exists yet, generate() fires once (a useRef guard — not useState — so React StrictMode's double-invoked effect can't double-fire; observed 2→1 generated_documents rows after the fix). A calm centered spinner state shows while it composes. (2) POLISHED DOC IS PRIMARY: the branded report-body article is always visible (previously print-only until finalized), rendered as a centered paper "sheet" with tightened typography (h2 underlines, token-based rhythm). (3) UGLY SOURCE BEHIND AN EXPAND BAR ABOVE: the raw Markdown textarea moved into a collapsed Collapsible ("Edit source" / "Edit narrative") above the document, with Copy Markdown + Save changes; it never competes with the polished view. (4) A clean toolbar (status left; Regenerate/Finalize/Download branded PDF right) replaces the scattered report-actions. Download now flushes unsaved edits first so the PDF always matches the on-screen doc. Applied consistently to both ReportPage and DeltaReportPage. Pure frontend + CSS; no server/scoring/schema/AI change (the PDF is still server-rendered from the same content_md). Verified: tsc + vitest 218 + build clean, and driven live in a browser against the seeded demo tenant (login → owner report auto-generates a single branded doc, expand bar reveals/copies the source, live preview updates; delta report same, with the score-movement headline + comparison table).
- 2026-07-18 | UI system consolidation — one source of truth for the repeated patterns (docs/26) | Consolidated the styling patterns so a pattern is defined once and reused, not reinvented per component. (1) EYEBROW LABEL: the uppercase micro-label was reimplemented ~17 times at drifting sizes/tracking (0.66–0.78rem, 0.03–0.08em). Added --eyebrow-size/-weight/-tracking tokens + a canonical .eyebrow class, and pointed every label class at them (stat-block-label, filter-label, tile-label, score-tile .tile-label, ws-chip-label, consensus-label, gallery-label, val-gap-label, diverge-cap, wb-score-label, advisory-detail-label, chain-status, cmp-outcome, owner-track, roadmap-startdate, .ui-table thead th, .cal-table th) — change the token, restyle every label. (2) TYPE SCALE: added --text-xs/-sm/-md (0.72/0.8/0.88) for the repeated sub-body sizes + .text-xs/-sm/-md utilities; snapped the four hardcoded inline fontSize spans onto them. (3) INLINE-STYLE REDUCTION: added .m-0/.mt-0 utilities and converted the safe margin one-offs (elements whose own class doesn't set margin — .muted/.form-error/bare headings), leaving margin-setting-class cases inline to avoid specificity regressions; inline styles 95→81. (4) docs/26-ui-system.md documents the canonical tokens (spacing, type, eyebrow, color), the prefer-these components (Card/SectionCard, PageHeader, StatBlock/StatRow, DataTable, Collapsible, EmptyState), and the format helpers (fmtCurrency/humanizeKey/formatFieldValue) so future code reuses instead of reinventing. Pure frontend + CSS; no routes/scoring/schema/AI change. Verified: tsc + vitest 225 + build clean, and rendered live in a browser across Portfolio/Engagement/Results/Valuation/Data room with no regression (all eyebrow labels now uniform).
- 2026-07-18 | Engineering-patterns foundation — agent-readable docs + copy-paste templates (docs/27) | With the repo grown to ~35 server functions, 25 migrations, 37 query hooks, audited the NON-UI repeated patterns and consolidated them as documented templates + checklists (deliberately not clever abstractions — CLAUDE.md ethos "boring, readable, no premature abstraction"; explicit is auditable for security-critical RLS). Findings: the firm-scoped RLS policy block repeats 38× across 18 migrations; a server function is wired in 3 places (authorize set → dispatch case → handler) + the frontend call; ~10 pages copied the same setBusy/try/catch/toast async-action block; 37 query hooks share one shape (already centralized via unwrap/selectOne); pure-module + fixture-test is the golden pattern. Built: (1) docs/27-engineering-patterns.md — the agent-readable catalog: for each "add a X" (table+RLS, server function, pure module, query hook, page) the template, the files it touches, and a definition-of-done, plus an "open consolidation candidates" section recording the deliberate NON-choices (RLS helper vs template — kept explicit for auditability; declarative function registry — revisit past ~35 fns; useAsyncAction incremental adoption). (2) templates/ — copy-paste skeletons (migration.sql, server-function.md, pure-module.ts+.test.ts, query-hook.ts, page.tsx, README) outside every tsconfig include so they don't compile. (3) src/lib/useAsyncAction.ts — the one concrete code consolidation: a hook that collapses the ~10-page async-action boilerplate (const {busy,run}=useAsyncAction()), documented as canonical, adopted incrementally. (4) CLAUDE.md now directs sessions to read docs/27 + copy from templates/ (and docs/26 for UI) before adding anything. Pure docs/templates + one additive hook; no routes/scoring/schema/AI/existing-code change. Verified: tsc + vitest 225 + build clean (templates uncompiled, hook typechecks).
- 2026-07-19 | Architecture map — visual system overview (docs/28) | Added docs/28-architecture-map.md, a diagram-first companion to the prose in docs/01, so the whole system is graspable at a glance now that the repo is large. Five mermaid diagrams (render on GitHub): (1) system layers — browser reads go direct via RLS, writes/compute go through the one router (dev emulator or prod Node service), external services (Anthropic/Stripe/ledger) are server-side only; (2) a compute request end-to-end — verify JWT → authorize (firm resolved from the profile, never the body) → entitlement gate → dispatch → handler under RLS; (3) the load-bearing rule — deterministic scoring engine (must match reference_scorer in CI) with AI as one-way narrative-only (never writes a score); (4) the tenancy ER spine (firm_id everywhere, immutable assessment snapshots per rubric_version); (5) the repo module map (src/ FE, server/ BE, shared/, supabase/migrations, seed/, dev/, scripts/, CI). Closes with a "where to go next" index to docs/26 (UI), docs/27 (patterns), docs/02/03/07 (data/scoring), docs/24/25/10 (path to production). Docs-only; no code/schema change.
- 2026-07-19 | Institutional register pass — elevate the UI to premium-B2B craft (styles.css + Sparkline + docs/26) | The system was already token-driven and consistent but read as good-SaaS, not institutional/"billion-dollar" software. Did a token-level register shift (validated in a font-accurate design lab against the real styles.css, in both themes) so it cascades everywhere instead of per-component: (1) GEOMETRY — --radius 12→8, --radius-sm 8→6, added --radius-lg 12; status/tier chips pill→rounded-rect (6px). Crisper than consumer-SaaS on purpose. (2) ELEVATION — border-defined: --shadow/--shadow-sm reduced to whispers (dropped the 20px ambient blur), added --shadow-lift as the one deliberate raise (hover/popovers), removed resting card shadows. (3) ENGINEERED NUMERALS — added --num token ('tnum' 1,'zero' 1) and applied font-variant-numeric: tabular-nums slashed-zero to every numeric class (stat values, table .num, deltas, count-pill, tile/dim/factor/wb/trajectory values) — the clearest financial-software typographic tell. (4) NEUTRALS — deepened/cooled --text-secondary (#566257→#4c574f), --text-muted (#86928a→#7a857c), --border (#dde3dd→#dadfda) so the accent is spent, not smeared into greys. (5) DATA SURFACE — DataTable densified (0.7→0.5rem cell padding, 0.88→0.84rem), engineered uppercase header with a stronger border-strong rule on --surface-1, whisper-tint (accent 5%) row hover; Sparkline component gained a gradient area fill + ringed endpoint dot so it reads as a chart, not a stray line. (6) CRAFT TELLS — tactile primary button (inset highlight + subtle top-light gradient + refined shadow), 2px accent focus ring, tinted ::selection, styled thin scrollbars. Documented as the "Institutional register — the craft bar" methodology in docs/26 (5 rules) + an "Institutional register pass" section at the foot of styles.css. Pure CSS + one presentational SVG change; no routes/scoring/schema/AI change. Verified: tsc -b clean, vite build clean, rendered light+dark in a font-accurate lab against the real stylesheet.
- 2026-07-19 | Composition system — well-composed page layouts, a reading path not a flat stack (styles.css + PageSection + EngagementPage + docs/26) | Continuation of the institutional pass, now at the layout/composition altitude. The pages were token-consistent but composed as a flat stack-lg of equal-weight full-width cards — masthead, tabs, and a dozen cards all read as peers with no reading path or primary/secondary hierarchy (validated in a font-accurate full-engagement-page design lab, both themes). Added a composition system (CSS primitives, cascades) + applied it to the flagship EngagementPage: (1) MASTHEAD BAND — .page-masthead groups PageHeader + the tab bar (EngagementNav) into one "where am I" unit, tightened (pageheader margin-bottom→space-4, eng-nav margin-top→0) and divided from content by section space, instead of the tabs floating as a separate stacked peer. (2) SECTION RHYTHM — .page-section major regions separated by --space-8, each introduced by a quiet eyebrow title + optional right-aligned note + a hairline rule (.page-section-head/-title/-note); this is the move that turns a flat stack into a composed page with a reading path. EngagementPage's ~12 blocks regrouped into three regions: "Readiness at a glance" (rail + snapshot/gaps + legs + trajectory), "Analysis & detail" (the folded collapsibles), "Record" (assessments + documents). (3) ALIGNED GUTTERS — .card-grid + legacy eng-grid/owner-grid/roadmap-cols all gutter on --gap-block so grids line up on one system; --col-min tunes wrap width. (4) MAIN+RAIL PRIMITIVE — .layout-rail (minmax(0,1fr) + fixed rail) available for lead-column pages. New reusable PageSection component (components/ui) so other pages adopt the spine trivially; documented as "Page composition" in docs/26 with the reference tsx. Pure CSS + additive component + one page restructure (no logic/data/scoring/AI change; RLS/immutability/rubric untouched). Verified: tsc -b clean, vite build clean, composed EngagementPage rendered light+dark in the lab against the real stylesheet, and re-verified the shipped styles.css (no override) reproduces it exactly.
- 2026-07-19 | Extend composition + labeling consistency (ResultsPage + Verification hints) | Rolled the composition spine (docs/26 "Page composition") onto ResultsPage — the other genuinely bare-region advisor page (like EngagementPage): masthead band (page-shell + page-masthead) then four labeled regions via PageSection — "Readiness" (bottom line + score summary + how-it's-built), "The six business areas", "Owner readiness", "What buyers would flag" (count moved into the section note). The former bare <section> h3+sub and the folded "Owner readiness" Collapsible became page regions; verified against the real stylesheet in the lab, light. Deliberately did NOT force PageSection onto Valuation/Verification/Owner/Dashboard — those are already composed at the card level (titled SectionCards/cards), and the aligned-gutter/rhythm tokens already cascade to them; adding region eyebrows over card titles would double-label. Labeling consistency: settled the StatBlock-hint convention as lowercase caption fragments (matching the Dashboard flagship) vs. capitalized descriptive Collapsible hints — reverted the Verification StatBlock hints to lowercase ("verified without review", etc.) so stat captions are uniform app-wide while keeping the de-cringed content. Pure JSX/copy; no logic/scoring/schema/AI change. Verified: tsc -b, vitest 134, vite build all clean.
- 2026-07-19 | Clicking-around UX consolidation — merge Evidence, sub-tab the Overview analysis stack, keep the engagement shell on assessment pages (docs/22 F2/F3/F4) | Acted on the three "once you're clicking around" frustrations from the software-testing review. (F2) The three Evidence surfaces — Data room, Documents, Verification — were merged into one tabbed EvidencePage: a single masthead carrying the rolled-up "% verified" number the whole stream summarizes to, plus a pill sub-tab switcher; the three original pages were refactored into panel components (DataRoomPanel/DocumentsPanel/VerificationPanel) that keep their own logic, and the old /data-room, /documents, /verification routes now redirect into /evidence/:section so bookmarks survive. EngagementNav collapses to a single "Evidence" tab; workstreams.ts points the Evidence stream at /evidence. (F4) The Overview "Analysis & detail" section was a vertical stack of seven collapsibles ("dropdowns"); it is now one SubTabs-driven panel (Score detail · Engagement log · How the plan connects · Deal outcome · Comparables · Compare · Setup & admin), tabs built conditionally from what the engagement has, one view at a time. (F3) The assessment-scoped pages (intake/results/workbench/report) rendered without the engagement frame, stranding the advisor mid-task; each now renders the PageHeader breadcrumbs + EngagementNav in a page-masthead, so the work-stream nav and a path back are always present (report's masthead is no-print). New shared presentational SubTabs component (+ CSS using real tokens). Presentation/IA + routing only — no scoring, schema, or AI behavior changed; tsc + vite build clean, 134 tests pass, and the flows were driven end-to-end in the demo (Evidence tabs deep-link, old routes redirect, all four assessment pages show eng-nav + breadcrumb).
- 2026-07-19 | Advisory Library authoring — edit / adapt / activate firm items, turning the catalog into something firms maintain (docs/21 Category B item 4) | The Advisory Library already let a firm create and delete its own items but stopped there — no way to edit an authored item, evolve a system item into a firm variant, or retire one without deleting. Built the missing authoring loop on LibraryPage (no schema change; the advisory_library_items RLS policy already grants advisors full CRUD on their own firm_id rows): (1) EDIT firm items — one reusable AuthoringForm now backs create/edit/adapt, remounted with a fresh key per target so it seeds cleanly; edit issues an UPDATE by id. (2) ADAPT a system item — an "Adapt for our firm" action on every system row opens the form prefilled from that item and saves an editable firm copy (source='advisor'), leaving the shared methodology row untouched — the intended "author on top of the system catalog" workflow (docs/20). (3) ACTIVATE/DEACTIVATE — firm items can be retired (active=false) without deleting; deactivated items stay in the catalog dimmed with an "Inactive" tag so they can be reactivated. Added a source filter (system / firm-authored) to the catalog. Non-negotiables intact: this is firm-tenant CRUD over rubric-adjacent catalog data, never scoring; no AI. tsc + vite build clean; drove the full loop in the demo (create 0→1, edit, deactivate shows Inactive, adapt a system item into a firm copy 1→2) with zero page errors.
- 2026-07-19 | Tab/group consistency — flatten the engagement nav to one even row; grouping lives only on the WorkstreamRail | The engagement tab bar encoded the five work-stream groups twice (rail labels + subtle nav dividers), and after Evidence merged from three tabs to one those dividers separated mostly single tabs — an arbitrary, lopsided grouping that also contradicted the component's own stated intent ("a clean, evenly-spaced row"). Made it consistent: EngagementNav now renders a single flat, evenly-spaced NavLink row (tabs still ordered by work stream for documentation, but no group wrappers), and the WorkstreamRail is the one place the grouping/taxonomy is drawn — one source of truth. Removed the now-dead .eng-nav-group / group-divider CSS; the nav uses uniform column-gap. The two intentional tab tiers stay distinct and each is used consistently: underline tabs for route navigation (app bar + engagement nav), pill SubTabs for in-page panel switching (Evidence sections + Overview analysis). Verified the nav renders identically across every page that shows it (Overview, Evidence, intake/results/workbench/report). Presentation only — no scoring/schema/AI; tsc + vite build clean, 134 tests pass.
- 2026-07-19 | Assessment "Path to 100" — make the whole gap from the current score to 100 legible and route it to remediation (ResultsPage + shared/scoring/interpret + styles.css) | The results page showed the DRS, the six dimension scores, and the fired-gap flags, but never the plain question an advisor and owner actually ask: how far is this from a perfect score, and where are the missing points? Added a deterministic gap decomposition and reframed the former "The six business areas" region as a "Path to 100" section. Non-negotiable respected — this adds NO new numbers and makes NO scoring decisions: it only re-reads weights/points the engine already produced. The identity (a dimension contributes drsWeight·score and could contribute drsWeight·100, so its recoverable points are drsWeight·(100−score); these sum to exactly 100−DRS) lives in two pure, fixture-adjacent helpers in shared/scoring/interpret.ts — gapToTarget (the whole gap split across the six dimensions, ranked largest-opportunity-first, with each dimension's share of the gap) and subScoreGaps (the measures inside a dimension ranked by the DRS points each would recover to full marks) — so the advisor page and the deterministic owner-report composer can never drift. UI: a gap-summary block (current→100 with a one-line "N points, M of them reach the next tier" caption) over a single composition bar (captured score + one segment per dimension sized to its open points, coloured by status) that shows "all the gaps to 100" at a glance; then the dimension breakdown ranked by recoverable points, each with a "+X open" chip and expanding to its measures showing "+X to DRS" per fix; landing on a remediation CTA that links straight to the engagement roadmap ("Build the remediation roadmap"). Presentation + one pure interpretation module (no routes/scoring/schema/AI/RLS change). Verified: tsc clean, vitest 139 pass (4 new gapToTarget/subScoreGaps cases asserting the sum-to-total-gap identity and the ranking), vite build clean.
- 2026-07-19 | CIM (Confidential Information Memorandum) — posture evidence collection toward the deal document and auto-generate it (docs/17 §5 DELIVERABLES) | The Evidence work stream assembles the buyer's diligence binder (the seven data-room sections) but had no destination that made its purpose legible, and the market-facing CIM had to be written by hand. Built the last step of the sell-side arc as one slice, keeping every non-negotiable: (1) POSTURE — a deterministic CIM section template (shared/cim/template.ts, code not a new table: it is document methodology, not a scored/versioned rubric) whose eight buyer-facing sections map onto the SAME data-room evidence taxonomy (docs/15 decision 4 — one taxonomy, never two), referencing the seven data-room sections FIN/OPS/HR/CUS/PIP/CMP/LEG exactly once between them. server/cim.ts rollupCimCoverage/buildCimCoverage rolls up the existing per-engagement data-room readiness into those sections (Ready/verified/missing), exposed read-only + ungated as cim-coverage; the CimPage leads with a CIM Readiness panel that shows per-section coverage and routes the advisor back to Evidence to collect what's missing (the "gap → go do the work" pattern). (2) GENERATE — a new 'cim' doc_type (one-line enum migration mirroring delta_report; reuses generated_documents, so advisor_firm_all RLS covers it and owner_report_read keeps owners out — CIM is staff-only). generateDocument dispatches to generateCim; buildCimPayload carries STRENGTHS AND VERIFIED FACTS ONLY (top dimensions' strong sub-score readings, adjusted EBITDA from the recast — never an enterprise value/asking price, never the DRS, never a gap), so neither the deterministic composer (composeCim, the numeral-firewall-safe fallback) nor the AI path (prompts/cim.v1.md, buyer-facing marketing tone) can surface a weakness. The existing numeral firewall applies unchanged. (3) DELIVER — CimPage mirrors the owner-report generate→edit→finalize→branded-PDF flow (render-cim-pdf via server/pdf.ts renderCimReportHtml); linked from Results ("CIM →") and Evidence ("Package into CIM →"). Refactored the shared Markdown renderer out of ReportPage into src/lib/markdown.tsx (used by both). AI stays narrative-only; no scoring/rubric/schema-beyond-the-enum change. Verified: tsc -b clean, vitest 140 (6 new: template completeness, coverage rollup math, composeCim numeral-firewall-clean + no weakness vocabulary), vite build clean.
- 2026-07-20 | Structural modularity — the six-engine function registry makes the platform-architecture engine model a property of the code (server/registry.ts + functions.ts + entitlements.ts + docs/27/28) | The platform-architecture doc (ExitBlueprintPlatformArchitecture §01) frames the system as six durable engines — Identity, Knowledge, Workflow, Rules, Reasoning, Collaboration — but that taxonomy lived only in prose; the compute router encoded nothing of it, and a function was wired in three drift-prone places (an authorize Set, a dispatch switch case, and its handler) with its engine nowhere in the code. Made the engines structural and inevitable by collapsing the three-place wiring into one declarative table (the "declarative function registry" consolidation candidate recorded in docs/27, now past the ~35-function trigger): server/registry.ts is REGISTRY = { '<name>': { engine, scope, gated?, handler } }, one entry per /functions/v1/* endpoint, grouped by engine. server/functions.ts is now the scope-driven GATEWAY only — handleFunctionCall looks up REGISTRY[name] and authorize() switches on the entry's declared AuthScope (a closed union with an exhaustiveness check, so adding a scope without a gate is a compile error). Security stays explicit and auditable: each scope is one hand-written authorization procedure (firm resolved from the caller's profile, never the body; RLS visibility check) — no dynamic dispatch, no body-shape guessing. Scopes were split finer than the old Sets (document→document-upload/review-queue/document, ledger→ledger-connect/ledger-complete) so each scope is exactly one procedure. entitlements.ts now DERIVES GATED_FNS from the registry's gated flags (single source of truth) instead of a hand-maintained parallel Set. Identity owns zero endpoints on purpose — it IS the gateway (authorize + RLS), documented as such. Behaviour is byte-for-byte unchanged: handleFunctionCall/FunctionContext/FunctionResult public surface preserved, gated set identical (the same 9 paid actions), every authorize branch reproduced. Added tests/registry.test.ts (engine/scope validity, identity-is-the-gateway, GATED_FNS === registry gated set, exactly-the-paid-actions, kebab-case names) and updated docs/27 (pattern §2 rewritten to "add one registry entry"; consolidation marked done) + docs/28 (§6 the six engines + registry, compute-request diagram, module map). No scoring/rubric/schema/RLS-policy/AI change. Verified against a real Postgres: migrate + seed clean, full vitest 243 pass (including functions.test.ts exercising every authorize branch, documents, sellside-review, ledger-oauth), rls-test 80/0 in CI order, tsc -b + vite build clean.
- 2026-07-20 | Beta UX & content polish — owner-readiness readings, actionable empty state, role-neutral login, owner-facing dimension labels | A polish pass ahead of the beta test group, targeting content quality and cross-linking without touching scoring/schema/AI. (1) CONTENT: three Owner Readiness Index readings in shared/scoring/interpret.ts produced broken or clipped sentences because they appended shared selectLabel fragments to stems that didn't fit — ORI-SEP rendered "Personal and business finances are mostly." (a fragment, no verb completion), ORI-OUTSIDE "Outside assets sufficient to retire: mostly.", ORI-DEBT "…align with the timeline: with minor issues." Each now maps its actual answer options (VAL-SEP fully|mostly|mixed, PFN-OUTSIDE yes|mostly|partially|no, PFN-DEBT yes|minor_issues|significant_issues) to complete, natural sentences, falling back to the old phrasing for any unmapped value. Because interpret.ts is the shared source for the advisor results page, the deterministic owner-report composer, and the PDF, all three surfaces improve together and can't drift. (2) CROSS-LINK: the engagement Overview "No documents yet" empty state named the next action ("Generate an owner report…") but was a dead end; it now carries a primary "Generate the owner report →" link to the latest assessment's report page when one exists. (3) FIRST IMPRESSION: the shared login page (advisors and owners both sign in there before role routing) was subtitled "Advisor workspace"; now the role-neutral "Sign in to your exit-readiness workspace." (4) MACHINE-ARTIFACT LEAK: the owner Learn page rendered a guide's raw dimension_code ("FIN") as a tag — forbidden by docs/26; it now maps the nine canonical rubric codes to their plain names (FIN→Financial Integrity …), keeping the raw code for authoring and the advisor Library. No scoring, rubric, schema, routing, RLS, or AI behavior changed. Verified: tsc -b + vite build clean, vitest 152 pass, and the ORI fixes + documents CTA driven end-to-end in the demo stack (results page owner-readiness now reads as full sentences).
- 2026-07-20 | Go-live infrastructure for exitblueprint.net — app/api subdomain topology, production owner-invite email, and a domain-specific runbook (docs/29) | The app was fully deploy-documented (docs/11/12/25) but nothing was specialized to the real domain, and the "one code change Phase 2 needs" (real owner-invite email) was still a dev-only direct insert. Chose the subdomain split app.exitblueprint.net (Vercel frontend) + api.exitblueprint.net (Render compute service) so the existing marketing site keeps the root exitblueprint.net untouched; Supabase stays at <ref>.supabase.co. (1) CODE: server/invite.ts now sends a real Supabase Auth admin inviteUserByEmail (set-password/magic link, redirecting to FUNCTIONS_ALLOWED_ORIGIN, override via OWNER_INVITE_REDIRECT_URL) when SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY are present, and falls back to the dev direct-insert path (returning dev_password) otherwise — so tests/dev are unchanged and the OwnerAccessCard's existing isDevStack-guarded dev-password note is correct in both modes. (2) CONFIG: render.yaml pins FUNCTIONS_ALLOWED_ORIGIN=https://app.exitblueprint.net (public, not a secret) and adds the required EB_DOCUMENT_KEY plus SUPABASE_SERVICE_ROLE_KEY (both sync:false); .env.example documents the new server vars. (3) DOCS: docs/29 is the exact ordered runbook (Supabase → Render → Vercel → DNS → connect → smoke test) with the two CNAME records (app→cname.vercel-dns.com, api→<svc>.onrender.com), leaving the root marketing DNS alone, and every env var's value for this domain. Provisioning (accounts, DNS, billing) is the operator's — this slice makes the repo production-correct and hands over the runbook. No scoring/rubric/schema/RLS/AI change. Verified: tsc -b + vite build clean, vitest 152 pass (91 DB-dependent skipped, incl. the unchanged invite path).
- 2026-07-20 | Clerk auth cutover — wire Clerk (Core 3, @clerk/react v6) as the identity provider, config-gated (docs/30) | docs/24 Slice 1 already moved the DB to a provider-agnostic identity (profiles.user_id text, app.user_* helpers read auth.jwt()->>'sub', firms.clerk_org_id, rls-test Clerk-shaped 80/80). This slice wires the app auth to Clerk without replacing the RLS model (Supabase trusts Clerk via third-party auth; Clerk Organizations = firms). Made it entirely config-gated on VITE_CLERK_PUBLISHABLE_KEY so the running Supabase-Auth beta, local dev, and CI are unchanged when the key is unset. Frontend: pluggable access-token source in supabase.ts (feeds supabase-js accessToken + the /functions Bearer), a Clerk-backed AuthProvider selected at build time (session/JWT from Clerk, unchanged profile/firm lookups, provider-agnostic {userId} session), ClerkProvider in main.tsx, Clerk <SignIn> on LoginPage, MFA gate satisfied under Clerk. Server: CLERK_JWKS_URL points the existing asymmetric verifier at Clerk; owner invites become Clerk organization invitations (scope in public_metadata) under CLERK_SECRET_KEY. Corrected the pasted quickstart on two points verified against live registry/types: the current package is @clerk/react (not the deprecated @clerk/clerk-react), and Vite must pass publishableKey to ClerkProvider (auto-read is Next.js-only). Remaining for the live Clerk app (docs/30): org/role setup, Supabase third-party-auth config, Vercel/Render env, and the organizationMembership.created provisioning webhook. Verified: tsc -b + vite build clean (validates Clerk API usage against real types), vitest 152 pass. No scoring/rubric/schema/RLS-policy/AI change.
- 2026-07-20 | Fix compute-service deploy timeout — decouple the deploy health gate from the DB and bound every DB call (server/http.ts + docs/12) | Render gates the deploy on healthCheckPath /health, and that handler awaited pool.query('select 1') on a pg.Pool created with no connectionTimeoutMillis (pg has no default connect timeout). When the DB was slow/unreachable at deploy time the query hung with no bound, the health request never responded, and Render marked the deploy "Timed Out" → SIGTERM → restart loop (exactly the observed logs: "listening on :10000" then "Timed Out"). Fixed in three parts, all defensive: (1) the pool now sets connectionTimeoutMillis: 10_000 so no DB call can hang forever; (2) /health became a pure LIVENESS probe — it returns 200 {ok:true} if the process is up, with zero external dependency, so a DB blip can never fail the deploy or kill a running instance (a restart loop never fixes a DB outage); (3) added a bounded READINESS probe /ready that does the DB check via a 5s withTimeout race, returning {ok:true,db:true}/200 or {ok:false,db:false}/503 for monitoring / a load balancer, never the deploy gate. Verified with an unreachable DB: /health answers 200 in ~5ms and /ready fails at the 5s bound instead of hanging (previously /health hung indefinitely). docs/12 §3 updated to describe the liveness/readiness split and to point Render at /health, not /ready. No scoring/rubric/schema/RLS/AI change. tsc -b + vite build clean, vitest 152 pass (91 DB-dependent skipped).
- 2026-07-20 | Clerk is the standard identity provider + automatic provisioning via scripts/admin.ts (docs/30) | The Clerk cutover (2026-07-20) landed config-gated so the Supabase-Auth beta stayed running; now that Vercel/Supabase/Render/Clerk are all live (clerk.exitblueprint.net), promoted Clerk from optional to standard and removed the hosted Supabase-Auth login. (1) PROVISIONING: new server/clerk.ts is the single thin fetch-based Clerk Backend API client (no @clerk/backend dep — follows the raw-fetch pattern already in invite.ts), exposing clerkEnabled/orgRoleForAppRole/splitName + createOrganization/findOrCreateUser/addMembership/createOrganizationInvitation. scripts/admin.ts now branches on CLERK_SECRET_KEY: create-firm creates the firm's Clerk Organization and stores firms.clerk_org_id (idempotent, backfills an existing firm); create-advisor creates/finds the Clerk user, adds the org membership (admin→org:admin else org:member — profiles.role stays the RLS source of truth), and writes the profile keyed to the Clerk user id (no auth.users row). Without the key it keeps the auth.users+profile dev path for local/CI seeding. (2) REMOVAL: server/invite.ts dropped the Supabase-admin inviteUserByEmail path (and the @supabase/supabase-js import), leaving Clerk org invitation (prod) + dev direct-insert; it now routes through server/clerk.ts. Frontend renamed SupabaseAuthProvider→DevAuthProvider (local/CI only), added requiresClerkConfig, and LoginPage shows a "not configured" panel for a hosted deploy without Clerk instead of a dead password form. (3) DOCS/CONFIG: CLAUDE.md Stack rule, docs/30 (config gate, provisioning section, rollback), and .env.example updated; hosted Supabase-Auth login is gone. No scoring/rubric/schema/RLS-policy/AI change. Verified: tsc typecheck (app + node) clean, vite build clean, vitest scoring/format/auth-jwt/clerk-provision green (DB- and Clerk-dependent suites skipped without DATABASE_URL/CLERK_SECRET_KEY, matching CI which supplies Postgres and the dev path).
- 2026-07-20 | Automatic Clerk provisioning webhook — POST /webhooks/clerk creates firms/advisors/owners on Clerk events (server/clerk-webhook.ts, docs/30 §5) | The admin.ts CLI provisions Clerk but a human has to run it; onboarding needs to be hands-off (end users won't run a CLI). Built the membership/organization webhook docs/30 §5 had only specced. server/clerk-webhook.ts has two pure-ish pieces: verifyClerkWebhook (Svix HMAC-SHA256 over `${svix-id}.${svix-timestamp}.${rawBody}` with the base64-decoded whsec secret, timing-safe over every v1 signature in the header — unit-tested against Svix's published test vector, not a self-referential one) and handleClerkEvent (service-role, RLS-bypass like admin.ts, idempotent). organization.created upserts the firms row + links clerk_org_id (converges with admin.ts create-firm and with orgs created straight in Clerk); organizationMembership.created inserts the profile (on conflict user_id do nothing), resolving firm_id from firms.clerk_org_id and reading app_role/company_id/full_name from the membership public_metadata — which Clerk copies from the accepted org invitation (confirmed in the BAPI spec: "the metadata will be transferred to the newly created organization membership"), the exact scope server/invite.ts sets. With no metadata (a member added directly in Clerk) the app role falls back from the Clerk org role (org:admin→admin, else advisor). Mounted at POST /webhooks/clerk in both server/http.ts (raw body, svix-timestamp recency check with 5-min tolerance, 200 handled/400 bad-sig/5xx transient-so-Svix-retries) and dev/supabase-dev-server.ts (so `clerk webhooks listen --forward-to .../webhooks/clerk` works locally). Gated on CLERK_WEBHOOK_SIGNING_SECRET (unset → 503, provisioning stays manual). Added CLERK_WEBHOOK_SIGNING_SECRET to .env.example + render.yaml, updated docs/30 §5 (built) + step 3/4, and tests/clerk-webhook.test.ts (5 signature-vector cases + 6 DB-gated provisioning cases). No scoring/rubric/schema/RLS-policy/AI change; no new dependency (node:crypto, not the svix package). Verified: tsc (app + node) clean, vite build clean, signature tests green (DB provisioning cases skip without DATABASE_URL, matching CI which supplies Postgres).
- 2026-07-20 | Compute service must never crash on a DB connection failure — top-level request guard + explicit DATABASE_URL pooler guidance (server/http.ts, render.yaml) | The Clerk webhook was returning 502 in production; Render logs showed the compute service crash-looping on `connect ENETUNREACH 2600:1f18:...:5432` — DATABASE_URL was Supabase's IPv6-only direct connection (db.<ref>.supabase.co:5432), which Render (no outbound IPv6) can't route. Root cause was operational (use the IPv4 session pooler), but the code turned it into an outage: every DB-backed route does `const c = await pool.connect()` OUTSIDE its try/catch, and the createServer callback had no top-level handler, so a rejected connect became an unhandled promise rejection and Node 24 terminated the process (restart loop → 502 on every delivery). Fix: extracted the request body into async handleRequest() and wrapped the createServer callback in `.catch()` that logs and replies 500 when headers aren't sent (else ends the response) — a single bad request or a DB blip now degrades to a clean 5xx (Svix/webhook callers retry; PDF/functions callers get an error) instead of killing the service for everyone. Mirrors the PR #58 principle (a DB blip must not take down the deploy/health path). Also hardened render.yaml's DATABASE_URL comment to name the IPv6/ENETUNREACH trap and require the postgres.<ref>@aws-0-<region>.pooler.supabase.com:5432 session pooler. Verified by running server/http.ts against an unreachable DB and delivering a validly-signed webhook: HTTP 500 returned and the process stayed alive across repeated deliveries (previously the first crashed it). No scoring/rubric/schema/RLS/AI change. tsc (app + node) + vite build clean, vitest 48 pass (6 DB-gated skipped).
- 2026-07-20 | Compute service TLS to the Supabase pooler — control pg ssl in code so the pooler cert doesn't break the connection (server/db-ssl.ts, server/http.ts) | After DATABASE_URL was switched to the Supabase IPv4 pooler (fixing ENETUNREACH), the service then failed with SELF_SIGNED_CERT_IN_CHAIN: the pooler cert chains to Supabase's private CA (not in Node's trust store) and recent pg treats an in-URL sslmode=require as verify-full. Verified against pg that an in-string sslmode overrides an explicit ssl option (client.ssl stays {} = verify), so the fix strips sslmode from the connection string and sets ssl in code. New server/db-ssl.ts resolveDbConnection(url, env): parses + removes sslmode, decides TLS (sslmode wins; else on for a remote host, off for localhost/CI), and returns ssl = {ca, rejectUnauthorized:true} when a CA is supplied (DATABASE_CA_CERT inline PEM or PGSSLROOTCERT path) else {rejectUnauthorized:false} (encrypted, unverified). server/http.ts builds the pool from the stripped connectionString + resolved ssl and warns in production when TLS is unverified (mirrors the EB_DOCUMENT_KEY nudge). Local dev/CI (no sslmode, localhost) get ssl:false — plain, unchanged. Documented DATABASE_CA_CERT in .env.example + render.yaml. Added tests/db-ssl.test.ts (6 pure cases: sslmode stripped, remote-unverified, CA-verified, local-plain, sslmode=disable, remote-default-on). Verified by booting server/http.ts with a sslmode=require pooler URL: it starts, logs the unverified-TLS warning, /health 200, no more verify-full alias warning (sslmode stripped). No scoring/rubric/schema/RLS/AI change. tsc (app + node) + vite build clean, vitest 48 pass (6 DB-gated skipped) + 6 new db-ssl.
- 2026-07-20 | Profile gate tolerates provisioning lag — retry the profile lookup before showing the "not set up" state (src/lib/auth.tsx, src/App.tsx) | With automatic Clerk-webhook provisioning, a brand-new user's profile row is written a beat AFTER first sign-in, and the Clerk→supabase access token needs a moment to attach (an unauthenticated read returns 0 rows under RLS). The single-shot useProfile lookup therefore missed and RequireAdvisor/RequireStaff flashed a terminal "This account has no advisor profile. Provision one with scripts/admin.ts." until a manual browser refresh re-queried. Fixed useProfile to retry the profiles read with backoff (attempts at 0/0.5/1/2/3.5s ≈ 7s total) and stop on the first hit — zero added latency for existing users (first attempt has no delay and breaks immediately), so only a missing profile waits. During the retry profileLoading stays true, so the gates show "Loading…" rather than the error, and the workspace renders as soon as the row lands. Also replaced the two developer-facing terminal messages (which told an end user to run a CLI — wrong now that provisioning is automatic) with a shared ProfileNotReady component: a plain-language "Your account isn't set up yet… give it a moment and refresh / contact your administrator" plus a Refresh button. No scoring/rubric/schema/RLS/AI change. tsc (app) + vite build clean.
- 2026-07-20 | Production go-live debug — structured loading/error UI system + Clerk↔Supabase auth diagnostic (src/lib/errors.ts, src/components/ui/{LoadingState,ErrorState,AsyncBoundary}, HealthPage, docs/26 §Loading & error states, docs/31) | First live session on app.exitblueprint.net surfaced two things: pervasive "database errors", and loading/error messages rendered as bare unstyled text. Root-caused the DB errors to the classic Clerk third-party-auth gap — every RLS policy targets `authenticated`, and PostgREST assumes the Postgres role from the JWT `role` claim, which only Clerk's Supabase integration injects; without it a signed-in token runs as `anon` and every firm-scoped read is denied (loads fine, reads empty/deny). That's dashboard config, not code (accessToken:()=>getToken() is already correct), so shipped diagnosis + hardening rather than a code "fix": (1) DIAGNOSTIC — /health now decodes the live session token and checks sub/issuer, `role: authenticated` (the decisive check, with the docs/31 fix pointer when missing), expiry, and an end-to-end authenticated read of the methodology tables; getAccessToken() exported from supabase.ts to support it. (2) NOISE — src/lib/auth.tsx profile/firm lookups .single()→.maybeSingle(): .single() 406s on 0 rows and the first-sign-in provisioning retry loop fired up to 5, spraying red DB errors in the console every fresh login. (3) UI SYSTEM (the explicit ask) — new LoadingState (page/section/inline), ErrorState (runs thrown values through describeError() which humanizes Supabase/PostgREST/network errors and adds the auth-config hint for RLS-shaped failures; page/section/inline), and AsyncBoundary (maps a TanStack Query result to loading→error→empty→content with retry=refetch). Replaced the App.tsx route-gate `<p className="muted page">Loading…</p>`, ProfileNotReady, and ~18 page-level `<p className="form-error">{raw}</p>` / plain-loading sites across the workspace; kept `.form-error` only for short field-level validation hints and the static "auth not configured" panel. Documented the rule in docs/26 + updated templates/page.tsx, and wrote docs/31 as the operator debug runbook. No scoring/rubric/schema/RLS-policy/AI change. Verified: tsc -b + vite build clean, vitest 176 pass (169 prior + 7 new errors.test.ts; 97 DB-gated skipped).
- 2026-07-20 | Admins are firm staff in the workspace — grant role='admin' the same firm-scoped RLS as advisors (supabase/migrations/20260720000100_admin_firm_access.sql, scripts/rls-test.ts, docs/31) | Live debugging of the app.exitblueprint.net "database errors" ended here: /health's new Profile linkage check showed the signed-in account had a correctly-keyed profile with role `admin`, and Firm-scoped read returned 0. Root cause: the frontend admits role='admin' to the advisor/staff surfaces (App.tsx RequireAdvisor/RequireStaff both allow admin), but every firm-scoped RLS policy gated on 'advisor' (or 'advisor'/'reviewer') only — the base RLS file even documented "admin: server-side only via service_role; no admin policies here" — so an admin signed in through the browser could read/write NOTHING firm-scoped (0-row reads, denied writes) → pervasive "database errors". Matthew chose to make admin a first-class firm-staff role for data access (matching what the frontend already assumes) rather than restrict provisioning. Implemented ADDITIVELY to eliminate regression risk: a new migration adds permissive admin_* policies alongside the existing advisor/staff policies (Postgres ORs permissive policies per command), so not one existing policy body changes and advisor/reviewer/owner access is byte-for-byte identical. Each admin policy MIRRORS the command and scoping of the advisor/staff policy it shadows — `for all` where staff have full access (30 firm_id-scoped tables + the 3 assessment-scoped child tables answers/sub_score_results/dimension_scores via the same exists() shape), SELECT-only on the audit/usage/billing tables staff only read (data_access_log/firm_subscriptions/llm_calls/usage_events), profiles firm read, agreement_versions insert, outcome_events append-only (select+insert) — all still gated on firm_id = app.user_firm_id(), so firm isolation is preserved. profiles.role stays the RLS source of truth; admin remains org:admin in Clerk for org management. Verified by applying the migration to a FRESH db and running rls-test: 87/87 pass (80 prior + 7 new admin checks proving full firm-A access AND zero firm-B read/write). No scoring/rubric/schema-shape/AI change; no existing RLS policy modified. tsc -b + vite build clean, vitest 176 pass.
- 2026-07-20 | Production hardening on all fronts (parallel) — Stripe billing, continuous-evaluation webhooks, observability, legal scaffold, frontend code-split + a11y | Brought the platform from "green CI, most of the architecture built" to "usable by real users end-to-end + institutional standards," per the platform-architecture PDF end state (stack: Vercel, Supabase, Render, Clerk, Stripe). Five workstreams built as disjoint modules and integrated centrally. Baseline before work: 273 tests / 87 RLS / eval 8/8 / build all green — additive hardening, not repair. No scoring/rubric/immutable-assessment/AI-boundary change anywhere.
- 2026-07-20 | Stripe billing — the missing keystone (server/stripe.ts, registry create-checkout-session/create-billing-portal-session, /webhooks/stripe, src/pages/BillingPage.tsx) | Schema (plans/firm_subscriptions/billing_events), the pure entitlement resolver (shared/entitlements.ts), the server-side gate (server/entitlements.ts + registry `gated`), and approved Solo/Practice/Firm tiers already existed — but nothing wrote firm_subscriptions, so no firm could ever become entitled. Built server/stripe.ts: a pure, SDK/DB-free applyStripeEvent(db, event) core (idempotent via billing_events unique stripe_event_id, on-conflict-do-nothing → duplicate no-op) handling checkout.session.completed / customer.subscription.created|updated|deleted / invoice.paid|payment_failed, mapping customer→firm via metadata.firm_id→client_reference_id→firms.stripe_customer_id; above it the env-gated SDK adapter (createCheckoutSession, createBillingPortalSession, verifyStripeSignature). Wired a raw-body signature-verified /webhooks/stripe in http.ts (mirrors the Clerk webhook: 200 handled/dup, 400 bad-sig, 503 unconfigured) and two registry endpoints — deliberately NOT gated (a lapsed firm must reach checkout/portal to restore access), scope 'firm', engine 'workflow' (billing has no dedicated engine — integrator's call, flagged for Matthew). BillingPage renders plan/status/seats/period + Manage-billing (portal) + plan-picker (checkout) + paywall empty state via the design system; Settings links to it. Env: STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, BILLING_ENFORCED (render.yaml + .env.example). Tests: tests/stripe.test.ts 11 (price→plan map, idempotency replay, every status transition, fake pg + hand-built events, no live Stripe/DB). Open for Matthew: monthly-vs-annual price ids (single stripe_price_id column today), trial policy (no trial created; resolver honors `trialing`), past_due→read-only grace-window cutover (resolver keeps past_due entitled), seat enforcement at invite (page displays X-of-N, doesn't block yet).
- 2026-07-20 | Continuous-evaluation webhooks (server/scheduled.ts, POST /webhooks/scheduled/{stale-engagements,stalled-tasks,reassessment-due}) | Realized the architecture doc §07 mechanism this repo was specced to expose: authenticated endpoints an external n8n instance calls on a schedule to ask "what changed / went quiet?" instead of waiting on a calendar. Three read-only, cross-firm (service-role) analyzers returning a uniform { generatedAt, thresholdDays, count, items[] } with firm_id per item for per-firm routing — stale engagements (no assessment/log activity in N=30d), stalled tasks (open + untouched N=14d or past due), reassessment due (last completed assessment older than N=90d). Read-only by construction: never writes a score, never mutates an immutable assessment (rules 4 & 5) — it surfaces that a re-assessment is due, it doesn't create one. Auth is a shared secret (x-webhook-secret vs WEBHOOK_SECRET) via a constant-time verifyWebhookSecret (length-guarded timingSafeEqual), NOT a user JWT — n8n is a trusted system caller like scripts/admin.ts. Query strings + row→item mappers exported pure for DB-free unit testing. tasks/engagements have no updated_at, so "untouched" is measured from created_at (documented). Wired in http.ts (503 when WEBHOOK_SECRET unset). Env: WEBHOOK_SECRET (render.yaml + .env.example). Tests: tests/scheduled.test.ts 15. No migration, no new table (stateless analyzers).
- 2026-07-20 | Observability — vendor-neutral error monitoring, no-op by default (server/observability.ts, src/lib/monitoring.ts, docs/32) | docs/24 §C4 called for Sentry on the frontend + Node service; there was none. Built a thin sink abstraction (ConsoleSink default / SentrySink adapter) so Sentry never leaks past one file: initObservability() reads SENTRY_DSN (no DSN → disabled, structured console only), captureError/captureMessage forward when enabled + always log a scrubbed structured line, logRequest emits a JSON access line. Frontend src/lib/monitoring.ts dynamic-imports @sentry/react only when VITE_SENTRY_DSN is set (the no-DSN build never pulls the SDK into the entry chunk) and exports MonitoringErrorBoundary. Scrubbing drops sensitive keys (token/secret/password/auth/cookie/jwt/email/body/…), keeps only scalars, caps strings — a request body can never ride along; sendDefaultPii:false. Wired: initObservability() + captureError in http.ts's three catch sites + res.on('finish') logRequest; initMonitoring() + a top-level MonitoringErrorBoundary (ErrorState page fallback + reload) around the app in main.tsx — a render crash or failed lazy chunk now recovers + reports instead of blanking #root. Env: SENTRY_DSN/SENTRY_ENVIRONMENT (render.yaml), VITE_SENTRY_DSN (Vercel build-time). Tests: tests/observability.test.ts 11 (disabled-by-default, sink flip, no-throw, scrubbing, parseable logs; no network).
- 2026-07-20 | Legal/trust scaffold (draft, pending counsel) + frontend a11y/bundle hardening (src/pages/legal/*, LegalFooter; App.tsx code-split, DataTable/styles/index.html/vite) | Legal: public /legal/{terms,privacy,dpa,subprocessors} pages built from a structured content module in the design system, each with an unmissable DRAFT banner and [to be completed by counsel] markers on every binding clause. Factual and safe as written: the six-vendor subprocessor register (Supabase/Vercel/Render/Clerk/Stripe/Anthropic, purpose+data category) and the security-measures paragraphs, drawn from docs/13/16 + CLAUDE.md — nothing invented (no jurisdictions, caps, or dates fabricated). LegalFooter linked from both app shells. MUST be reviewed by counsel before publishing. Frontend (from a read-only audit): route-level React.lazy + Suspense for all ~30 pages (login eager), Vite manualChunks for react/clerk/supabase/query, and /dev/* routes gated to import.meta.env.DEV — entry bundle dropped from one 826 KB chunk (237 KB gzip) to a ~163 KB-gzip entry + split vendor/route chunks. A11y: DataTable clickable rows made keyboard-operable (role/tabIndex/Enter+Space — propagates to every table), --text-muted darkened to clear WCAG AA 4.5:1 in both themes, index.html given description/theme-color/inline-SVG favicon. Tests: tests/legal.test.ts 10. Verified end-to-end on a FRESH db: migrate → rls 87/87 → seed ×2 → seed:demo ×2 → vitest 320 → eval 8/8 → build, all green; tsc (app + node) clean.
- 2026-07-20 | Firm provisioning seeds the default engagement agreement — fixes the unreachable-blocker onboarding state (shared/agreement-template.ts, server/agreements.ts ensureDefaultAgreementVersion, server/clerk-webhook.ts, scripts/admin.ts, src/pages/ClientsPage.tsx) | Live prod debugging traced "prod is empty / can't start anything" to a real onboarding gap: creating an engagement requires an active agreement_versions row for the firm (createEngagementWithAgreement + ClientsPage both block on it), but agreement versions were only created by the separate `create-agreement-version` CLI — neither the Clerk webhook provisionFirm nor admin.ts create-firm seeded one, and there is no in-app UI to add one. So any firm born from Clerk self-serve/provisioning could never start an engagement, and the empty-state copy pointed at a nonexistent "firm settings" screen. Fix: moved the canonical DEFAULT_AGREEMENT_* template to shared/ (the Docker image ships shared/ but not scripts/, so server code must source it there; scripts/agreement-template.ts now re-exports it) and added ensureDefaultAgreementVersion(db, firmId) — an idempotent (on conflict (firm_id, version_label) do nothing) service-role insert of the active EA-1.0 default — called from provisionFirm (all three paths, also backfilling a pre-existing firm) and admin.ts createFirm (dev + Clerk paths). New firms start on the platform default and replace it later via the CLI (Matthew's call: default-first, not require-own). Corrected the ClientsPage empty-state copy. Extended tests/clerk-webhook.test.ts to assert the agreement is seeded on organization.created (+ cleanup deletes agreement_versions before firms for the new FK). No scoring/rubric/immutable-assessment/RLS-policy/AI change. Verified on a FRESH db: migrate → rls 87/87 → seed ×2 → seed:demo ×2 → vitest 320 → eval 8/8 → build, all green; tsc (app + node) clean.
- 2026-07-20 | Fix "we couldn't reach the server" across function-backed views — CORS allowlist was emitted as an invalid header + a coding TypeError was mislabeled as an outage (server/cors.ts, server/http.ts, src/lib/errors.ts) | Prod reported a blanket "we couldn't reach the server" in multiple places. Root cause: server/http.ts documented FUNCTIONS_ALLOWED_ORIGIN as a comma-separated allowlist but set the raw env string verbatim as Access-Control-Allow-Origin — a header that may only be a single origin or '*', never a comma-joined list. So the moment prod tightened CORS to more than one origin (apex + www), every browser rejected the preflight and all function calls (explain/valuation/comparables/verification/education/advisory/cim-coverage/compare — the core assessment+report views) failed as fetch TypeErrors, while plain Supabase REST reads still worked. Fix: extracted a pure server/cors.ts (parseAllowedOrigins + resolveCorsOrigin) that parses the list and echoes back the request's own Origin when it's allowlisted (falls back to the first configured origin for a non-listed/missing Origin, adds Vary: Origin), wired into setCors(req,res). Second, honesty fix: describeError treated ANY TypeError as network ("check your connection"), so a plain coding TypeError (e.g. "Cannot read properties of undefined") was disguised as a server outage; narrowed it to match the recognizable fetch-failure messages (Chrome/Firefox/Safari/undici) so real bugs surface instead of hiding. No scoring/rubric/immutable-assessment/RLS/AI change. Tests: tests/cors.test.ts 6 (default-any, comma split, per-request echo, non-listed fallback, never comma-joined) + tests/errors.test.ts extended (all-browser fetch messages → network; coding TypeError → not network). tsc (app + node) clean, vitest 230 pass.
- 2026-07-20 | Production design-review polish pass (docs/33) | Elevated shipped UI without redesign: fixed owner reports rendering raw markdown, owner errors masquerading as empty states, ConfirmDialog focus trap, sticky headers under the app bar, DataTable/SubTabs ARIA, unguarded destructive disconnects, mis-semantic doc status colors, composition-spine drift on Dashboard/Valuation/Roadmap/BuyerLens, and raw-enum/snake_case leaks (incl. a doc_type truncation bug). Identity tokens/type/spacing/component library preserved.
- 2026-07-20 | ResultsPage keeps its rich <details>/<summary> dimension disclosures (sanctioned exception to "use Collapsible") | The summary carries structured content (rank, progress track, open chip) Collapsible's title slot can't host cleanly; the app-wide summary:focus-visible ring now gives it the same keyboard affordance, so a rewrite wasn't justified.
- 2026-07-20 | Diagnose the real "we couldn't reach the server": VITE_FUNCTIONS_URL unset in prod (src/lib/supabase.ts, src/pages/HealthPage.tsx) | The prior CORS-header fix (#68) addressed a real latent bug but was NOT this incident. Live devtools showed function POSTs going to https://<ref>.supabase.co/functions/v1/list-review-queue (the Supabase URL), blocked at the CORS preflight — because VITE_FUNCTIONS_URL was unset in the Vercel build, so invokeFunction fell back to VITE_SUPABASE_URL (supabase.ts: `functionsUrl = VITE_FUNCTIONS_URL || VITE_SUPABASE_URL`). Supabase's Edge runtime doesn't serve this app's functions (they run on the separate Render compute service at api.exitblueprint.net), so every function-backed view failed while plain Supabase REST reads worked — exactly the "multiple places" pattern. docs/29 §3 already warned "never leave VITE_FUNCTIONS_URL unset in production"; the failure surfaced only as an opaque browser CORS error with no in-app pointer. Immediate fix is ops (set VITE_FUNCTIONS_URL=https://api.exitblueprint.net in Vercel + redeploy — VITE_ vars bake in at build). Durable guard (this change): a hosted build (real VITE_SUPABASE_URL) with no VITE_FUNCTIONS_URL now console.errors a precise pointer on load (next to the CORS errors it causes), and /health gained a "Functions service" check that fails with the same message when the var is unset, else pings the resolved <functionsUrl>/health to prove reachability (dev = same-origin, skipped). No scoring/rubric/immutable-assessment/RLS/AI change. tsc (app + node) clean, vitest 230 pass, build green.
- 2026-07-20 | admin is firm staff at the function layer too — the #65 gap one layer deeper (server/functions.ts) | Live prod debugging: a signed-in role=admin hit "Something went wrong / advisor or reviewer profile required" on the review queue. #65 had granted admins firm-scoped RLS and the frontend routes them to the staff surfaces (RequireAdvisor admits advisor+admin; RequireStaff admits advisor+reviewer+admin), but the function-layer authorize() role lists were never updated — the staff scopes checked ['advisor','reviewer'] and create-engagement checked ['advisor'], so firmFromProfile returned null for an admin and every staff/advisor function 403'd even though the frontend and RLS both admit them. Fix mirrors #65 exactly (additive, RLS still the boundary): staff scopes (review-queue, document, document-upload, sellside-engagement, sellside-item) → ['advisor','reviewer','admin']; create-engagement → ['advisor','admin']. firmFromProfile still resolves through asUser, so this grants no access beyond what admin_* RLS policies already permit — it stops the function layer from rejecting a caller the rest of the stack accepts. tests/functions.test.ts: added an admin→list-review-queue 200 case (DB-gated). No scoring/rubric/immutable-assessment/AI change. tsc (app + node) clean, vitest 230 pass (98 DB-gated skipped without DATABASE_URL).
- 2026-07-20 | In-system methodology loading — a hosted beta seeds itself, no external `npm run db:seed` (server/seed-methodology.ts, server/platform-admin.ts, registry `seed-methodology` + `platform-admin` scope, server/Dockerfile ships /seed, src/pages/HealthPage.tsx) | Live debugging landed on the real beta blocker: every /health check green (auth/RLS/profile all OK) yet "no active rubric version — run npm run db:seed" on starting a baseline assessment. Root cause: a Vercel deploy ships only the frontend and never runs migrations or the seed, so the hosted Supabase had schema + tenant data (firm, 2 companies, admin profile) but no methodology — rubric_versions empty. The only way to load it was Matthew running the CLI with a production connection string, which he asked to eliminate (also the forward path for future rubric refinement from outcome data). Built the in-system loader honoring the non-negotiables: extracted the seed pipeline (load → validate → write → verify counts) out of scripts/seed.ts into server/seed-methodology.ts so the SAME validated, idempotent logic runs from the CLI and from a new superadmin-gated `seed-methodology` compute function; scripts/seed.ts is now a thin CLI wrapper over it (byte-identical console output; CI's seed-twice idempotency + reference-scorer validation unchanged). Authorization is a NEW tier above firm admin: rubric_versions is GLOBAL (no firm_id), so publishing methodology must never be reachable through a firm-scoped role — added a `platform-admin` auth scope gated by an env allowlist of Clerk ids (PLATFORM_SUPERADMIN_IDS, default-deny, checked in functions.ts authorize(); NOT a profiles.role, no schema change, reversible). The function runs with the service-role client (RLS bypass — methodology tables have read-only RLS and no firm_id) and is idempotent. server/Dockerfile now COPYs /seed into the image (it previously shipped only server/ + shared/, so the compute service couldn't read the CSVs). HealthPage gained a "Methodology" check (active-rubric presence, distinct from the readability check) and, when absent, a superadmin "Load methodology" button that calls the function and renders the per-table report — the page that diagnoses the problem now offers the one-click fix. AI never involved; the rubric stays deterministic data loaded from canonical /seed. No scoring/rubric-content/immutable-assessment/RLS-policy change. Verified on a fresh db: migrate → seed ×2 (insert then idempotent update, active DRS-1.0 present) → seed:demo (validates vs reference scorer) → vitest 328 pass → build; plus a direct gateway check (non-superadmin → 403 "platform superadmin required", superadmin → 200 ok:true, 15 tables). To bootstrap the beta: set PLATFORM_SUPERADMIN_IDS on the Render compute service to Matthew's Clerk id, redeploy so /seed ships, then click Load methodology on /health.
- 2026-07-20 | CLM completeness: engagement lifecycle management + hard delete (server/engagements.ts, registry `delete-engagement` + `delete-engagement` auth scope, EngagementPage Setup & admin, ConfirmDialog `confirmDisabled`) | The engagement was creatable and readable but never manageable: its status (active|paused|exited|churned) was display-only with no control, and there was no way to remove one — a mis-created engagement or a client "delete our data" request had no path. Added the soft path (a Status control on the Setup & admin tab; a plain firm-scoped UPDATE under the existing advisor RLS) and the destructive path (a real hard delete, not an archive). Hard delete MUST be a service-role function: an engagement is the root of a deep tree whose FKs are deliberately not ON DELETE CASCADE (nothing but this teardown may remove client history), and the immutability triggers (#20260718000200) plus append-only grants (outcome_events, llm_calls, data_access_log) mean an advisor JWT running as `authenticated` cannot tear it down — the completed-assessment freeze exempts only service_role/maintenance roles, which is exactly why it lives server-side. deleteEngagement() runs one transaction of FK-safe ordered deletes across every engagement-child table (sell-side graph, documents+blobs, valuation, data room, outcomes, roadmap/log/instrumentation, the assessment sub-tree, agreement), then a DYNAMIC orphan check (information_schema for any public table with an engagement_id column still holding rows) so a future table added without updating the teardown fails loudly and rolls back rather than orphaning data; the removal is audited into data_access_log with engagement_id null + ids in detail so the record survives its own subject. Scope `delete-engagement` restricts it to advisor/admin (owners/reviewers can see an engagement but must not delete it) and is deliberately un-gated (removing data must never be blocked by a lapsed subscription). UI gates it behind a harsh, type-the-client-name confirmation that itemizes exactly what will be destroyed (assessments+score history, gaps, tasks, documents, valuation, the un-re-derivable deal-outcome calibration record, …). No scoring/rubric/immutable-assessment/RLS-policy change. Tests: tests/functions.test.ts builds an engagement loaded with the tricky-ordering children (gap→assessment, task+milestone→gap, deal_outcome→assessment, doc+blob, append-only outcome_events) and asserts one call empties every table + writes the audit row, plus advisor/admin-only (403) and foreign-engagement (404) denials. Verified against a fresh migrated+seeded Postgres 16: vitest 331 pass (all DB-gated included), tsc (app + node) clean.
- 2026-07-20 | Auth session-lifecycle UX pass — recover from mid-session expiry, warn before idle logout, return to intent after login, theme the Clerk widget (src/lib/sessionExpiry.ts, src/lib/auth.tsx, src/App.tsx, src/components/ui/{ErrorState,AsyncBoundary,IdleWarningModal}.tsx, src/pages/LoginPage.tsx) | The auth seam handled cold-load well (spinner-before-session, maybeSingle profile backoff) but dropped off after the happy path — the "clunk" on the identity boundary. Four fixes, all extending existing patterns. (1) Mid-session expiry recovery: errors.ts already classified a stale/invalid JWT as kind:'auth' but nothing acted on it — the QueryClient had no onError, and AsyncBoundary wired a refetch "Try again" that re-failed with the dead token (a retry-of-death). Added a QueryCache onError that runs describeError and, on kind:'auth', calls notifySessionExpired() (new src/lib/sessionExpiry.ts — a React-free bridge the module-level QueryClient uses to reach the AuthProvider, re-entrancy-guarded so a storm of failing queries yields one sign-out). Both providers register a handler that signs out; the gates then route to /login. AsyncBoundary now only wires the refetch retry when describeError().retryable, and ErrorState renders a real "Sign in again" action (→ notifySessionExpired) for kind:'auth' instead of a dead card. (2) Idle logout was silent — useIdleTimeout signed out at 30min with no notice; refactored to two-phase (warn 60s before with a live countdown via new IdleWarningModal reusing the .ui-modal surface; any activity or "Stay signed in" resets), and /login now explains why via a one-shot sessionStorage reason (idle vs expired). (3) Deep-link return: gates already stashed location.state.from but LoginPage discarded it — now reads it into returnTo and passes it to navigate() (dev) and Clerk <SignIn forceRedirectUrl> (prod). (4) Clerk <SignIn> ignored the app theme — added appearance built from the app's own CSS tokens (read via getComputedStyle, recomputed on theme change) so the widget matches chrome in light/dark. Guarded a StrictMode pitfall: the signout-reason read is a pure peek during render + a cancellable clear in an effect, so the dev double-mount doesn't wipe the notice (would have misfired in prod too). No scoring/rubric/immutable-assessment/RLS/AI change. Tests: new tests/sessionExpiry.test.ts (handler dispatch, storm-collapse + re-arm, one-shot reason); tsc clean, vitest 234 pass, build green; login notice + Clerk theming driven live in a browser (dark/light).
- 2026-07-20 | Engagement Overview declutter — tighten the at-a-glance region and drop redundant blocks (src/pages/EngagementPage.tsx, src/styles.css) | The Overview's first screen carried more than a glance needs. Removed the "Readiness at a glance" PageSection header so the WorkstreamRail + Current-readiness/Open-gaps cards sit directly under the nav tabs (plain page-section, no eyebrow/rule). Capped the "Open gaps to remediate" card at GAP_PREVIEW (5) with a "Buyer lens →" header action mirroring Current readiness's "Full results →", plus a "+N more in the Buyer lens" link when truncated — the full worklist lives on the Buyer lens tab, so the Overview stays a summary. Removed the "Readiness alignment" (CEPA three-leg) Card as redundant with the score/tier/ORI already shown. Under Analysis & detail, removed the "Four intangible capitals" lens from the Score-detail tab, leaving the two contribution/divergence cards. buildAlignment/AlignmentLeg and rollUpCapitals imports dropped from the page (lib + their tests untouched). Presentation-only: no scoring/rubric/immutable-assessment/RLS/AI change. tsc (app + node) clean.
- 2026-07-20 | UI cleanup pass 1 — Evidence guidance + Review-queue visibility + Security→Settings merge + Clerk portal links (src/pages/EvidencePage.tsx, DataRoomPage unchanged, src/App.tsx, src/pages/SettingsPage.tsx, deleted src/pages/SecurityPage.tsx, src/styles.css) | The Evidence surface showed a big checklist and a verification % with no explanation of how the two connect, so an advisor had no sense of how to move the number from 0→100; added a 4-step "how the binder works" stepper (Build the data room → Uploads go to review → Facts get verified → Package into CIM) above the sub-tabs, with the review step linking to /review — naming the previously-invisible hand-off. Shortened the Evidence subtitle ("The diligence binder — request list, source documents, and what's proven. N% verified.") so the "Package into CIM →" header action no longer wraps below it (the flex header wrapped actions under a long subtitle). Made the review queue read as an active inbox: the top-nav Review link now carries a pending-count badge (ReviewNavLink, a 60s-stale list-review-queue count). Merged the standalone Security page into Settings per Matthew's note (it was bland and its data-protection summary is now covered by the legal footer): the MFA enroll/verify flow moved verbatim into a Settings MfaCard, the /security nav link and route were removed (route now redirects to /settings), and the MFA gate (RequireAdvisor/useMfaGate) sends un-enrolled advisors to /settings instead. Added a Clerk-only Account & organization card in Settings (openUserProfile/openOrganizationProfile) so account/MFA-device/org management is reachable from EB settings; clarified that the client-facing report logo is set in EB settings independent of any Clerk logo. Presentation/navigation only — no scoring/rubric/immutable-assessment/RLS/AI change. tsc (app+node) clean, vitest 234 pass.
- 2026-07-20 | Roadmap: concrete target sale date + compact workstream Gantt + reorder + next-up-by-party (migration 20260720000200, src/lib/queries.ts, src/pages/{RoadmapPage,EngagementPage}.tsx, src/styles.css) | The "On pace for the exit window?" chart ran to a fuzzy date derived from the coarse target_exit_window band ("24-36 months" → start + earliest month), so the required-pace line never mapped to the owner's actual deadline. Added a nullable engagements.target_close_date (no RLS change — existing advisor/admin UPDATE policies cover it) set as a "Target sale date" input on the Roadmap toolbar (beside Start date, as requested) and on the Engagement Setup tab; targetExitDate() now prefers it and falls back to the window, so the exit-pace chart and roadmap share one real deadline. Rebuilt the roadmap Gantt, which was "messy and too long": it rendered one row per task across two swimlanes (dozens of rows). It now draws ONE bar per workstream (playbook) spanning start→that workstream's last due date — the window in which its gaps close — labelled with progress and the next task, ordered by nearest due; personal + business milestones keep their diamonds and the target sale date is its own diamond so the whole plan visibly runs to it. Promoted the deal-team handoff out of a disclosure into an always-visible "Next up by party" card grid showing each party's next task + open count (was "display next task for each party involved"). Replaced the date-bucketed 90-day sprint list with a single sequence-ordered open-task list carrying ↑/↓ reorder controls (swapTaskOrder persists the sequence swap) so advisors can customise order; due dates remain editable inline. Dropped now-unused groupIntoSprints/Collapsible imports (lib fn + tests untouched). No scoring/rubric/immutable-assessment/AI change. tsc (app+node) clean, vitest 234 pass.
- 2026-07-20 | UI cleanup pass 2 — cleaner billing plan cards + report/buyer-lens spacing + reachable engagement delete (src/pages/{BillingPage,EngagementPage,BuyerLensPage}.tsx, src/styles.css) | Billing looked bare — plan cards showed only name/seats and hid the features array entirely; rebuilt them as a pricing-table grid (FEATURE_LABEL maps the capability keys to human labels, rendered as a checkmark list), highlighting the current plan with an accent ring and a full-width CTA, plus a "prices shown at checkout / Stripe handles cards" note (no price column exists — pricing lives in Stripe). Fixed the reported spacing: BuyerLens dropped its per-section negative-margin blurb hack (marginTop:-0.35rem) for the design-system .section-sub class; the Report page's container had NO styles, so masthead/toolbar/source-editor/sheet stacked on mismatched ad-hoc margins — gave .report one uniform flex-column rhythm (gap space-5) and neutralised the old margins (masthead margin-bottom, toolbar margin-bottom, sheet-wrap top pad) that fought it. Fixed the real "can't delete an engagement" bug: the Setup & admin tab (status control + hard-delete Danger zone) rendered ONLY inside the completed-assessment branch, so a freshly-created / mis-created engagement — precisely the one you'd want to delete — had no reachable delete or status control at all. Extracted that block into a `setupAdmin` element rendered both in the analysis subtab AND, when there are no completed assessments, as its own always-present "Setup & admin" section. No scoring/rubric/immutable-assessment/RLS/AI change. tsc (app+node) clean, vitest 234 pass, production build green.
- 2026-07-20 | Roadmap reorder correctness — dedicated tasks.display_order (migration 20260720000300, src/lib/queries.ts TaskRow, src/pages/RoadmapPage.tsx) | The first cut of the roadmap reorder swapped tasks.sequence, which is wrong: `sequence` is the per-playbook template position and forms the idempotency key (playbook_id:sequence) server/roadmap.ts uses to skip already-generated tasks, so a swap would (a) no-op whenever two adjacent tasks shared a sequence value — sequences repeat across playbooks — and (b) on the next reschedule make generation re-insert duplicate tasks. Added a dedicated nullable tasks.display_order column instead; the ↑/↓ controls now call moveTask(), which densely renumbers the open list (0..n) with the move applied and writes only the changed rows, so a reorder always takes effect and never touches `sequence`. openOrdered sorts by display_order (nulls last, then due date) so a freshly generated plan reads by due date until an advisor reorders it. No RLS change (existing tasks policies cover the column). tsc clean, vitest 234 pass.
- 2026-07-20 | Self-serve firm team management — invite-advisor + a Team card in Settings (docs/35 blocker #1) | The #1 beta blocker (docs/35 Phase 8) was that firms/advisors could only be provisioned by a developer running scripts/admin.ts create-advisor with a Clerk secret — no in-app path, so open beta was blocked and even a pilot was hand-provisioned. Built the self-serve counterpart: server/invite-advisor.ts inviteAdvisor(db, firmId, email, fullName, role) mirrors server/invite.ts inviteOwner's two-path shape — CLERK (send a Clerk organization invitation carrying app_role/firm_id in public_metadata; the membership-created webhook provisions the profile, docs/30 §5) and DEV (insert the auth.users row + staff profile, returns dev_password 'demo') — restricted to staff roles advisor|reviewer|admin (owners are invited through the engagement flow, never here). Registered as the invite-advisor function under scope 'firm' (the caller's firm is resolved from their advisor/admin profile, never the body — a firm can only grow its own team) in the collaboration engine; deliberately NOT entitlement-gated (managing your team is never blocked by billing state). Seat enforcement honors the plan seat_limit only when BILLING_ENFORCED=true (matching every other billing gate; comped/no-plan firms are unlimited), and the seat USAGE is always returned so the UI shows "N of M seats". Imports the PURE shared/entitlements resolver (not server/entitlements, which imports the registry) to avoid an import cycle that would leave GATED_FNS uninitialized. Frontend: a Team SectionCard on SettingsPage lists firm staff (read directly under RLS via advisor_firm_profiles_read/admin_firm_profiles_read, "· you" marker, role chips), an invite form (email/name/role), the seat-usage line, and the dev-stack invite note. No scoring/rubric/immutable-assessment/RLS-policy/AI change (rules 1–6 intact) — this is provisioning + IA over the existing Clerk-org/profiles model. Verified fresh in CI order: migrate → rls 87/87 → seed×2 → seed:demo → vitest 342 (+7: tests/invite-advisor.test.ts dev-path incl. email/role validation, idempotency, seat usage; tests/functions.test.ts +2 router authorize — advisor invites a colleague 200, non-staff 403) → tsc -b + build clean, and driven live in a browser against the demo (Team card renders, inviting a reviewer moved "1 seat used" → "2 seats used" and the new member appeared with a Reviewer chip).
- 2026-07-20 | Self-serve engagement data export — the export half of docs/35 Phase 9 ("self-serve data export/purge") | The purge half shipped earlier (deleteEngagement, #129); this adds the missing export half so a firm can take a full, portable copy of an engagement's data out of the platform — for a client's "give us our data" request, a backup before deletion, or migration — without a developer running SQL. server/export.ts exportEngagement(db, firmId, engagementId) reads the engagement's business substance (company, assessments + their answers/dimension_scores/sub_score_results, gaps, tasks, roadmap_milestones, valuation_inputs + ebitda_recasts, data-room items, deal/engagement outcomes, engagement_log, and the generated report TEXT) into one stable JSON envelope (schema_version 1, generated_at, per-collection counts). Documents are exported as METADATA ONLY (id/category/original_filename/mime_type/status) — the encrypted bytes are intentionally excluded (large, binary, individually downloadable via the signed-URL path); internal telemetry (usage_events, data_access_log, llm_calls) is operational, not engagement data, and is excluded. Registered as the export-engagement function under a new AuthScope 'export-engagement' with the SAME authorization as delete (advisor/admin staff who own the engagement — the person who can remove the data can export it; the closed-union exhaustiveness check made adding the scope a one-case edit in functions.ts authorize), engine 'workflow', and deliberately NOT entitlement-gated (taking your own data out must never be blocked by a lapsed subscription, mirroring delete). Read-only — never writes or mutates anything (rules 1–5 untouched). Frontend: an "Export data" SectionCard on the engagement Setup & admin tab, above Delete, that calls export-engagement and downloads exitblueprint-<company>-<date>.json (Blob). Verified fresh in CI order: migrate → rls 87/87 → seed×2 → seed:demo → vitest 345 (+3 tests/export.test.ts: envelope + log capture, documents-metadata-only, foreign-firm refusal; registry VALID_SCOPES +export-engagement) → tsc -b + build clean, and driven live in a browser against the demo (downloaded a valid 2-assessment/10-gap export for Cascade Facility Services; documents carried no bytes column; no app errors).
- 2026-07-20 | Move Security out of the primary nav into an account menu (docs/34 MEDIUM) | docs/34's remaining MEDIUM finding: "Security" — a once-only MFA/account setup screen — sat in the advisor primary nav as a daily-workflow peer of Engagements/Review/Library/Settings (Hick's-Law load; an account concern masquerading as a destination). It was deferred there for needing "a new menu affordance + focus management; the MFA gate route must stay reachable." Built that affordance: a new accessible account menu (src/components/UserMenu.tsx) hung off the app-bar user chip (avatar + email + caret) following the WAI-ARIA menu-button pattern — trigger with aria-haspopup/aria-expanded, a role="menu" panel of role="menuitem" links, opening focuses the first item, Escape closes and returns focus to the trigger, an outside click or focus leaving the menu closes it, and Up/Down rove between items. Security and Sign out moved into it; the primary nav drops to four true destinations (Engagements, Review, Library, Settings). The /security route is unchanged and fully reachable (the MFA gate still redirects there), so the enforcement path is intact — this is pure IA + a menu component. Presentation only; no scoring/schema/RLS/AI/routing-target change. Verified: tsc -b + vite build clean, and driven live in a browser against the demo (Security absent from the primary nav; the menu opens with Security + Sign out, Escape closes it, and clicking Security navigates to /security; no console errors).
- 2026-07-20 | In-app "Needs attention" surface — an in-product worklist reusing the continuous-eval analyzers (docs/35 Phase 9) | docs/35 Phase 9 flagged "in-app scheduling/notifications" as missing: the three continuous-evaluation analyzers (server/scheduled.ts — reassessment due >90d, stalled tasks open/untouched 14d or past due, stale engagements no activity 30d) only powered external n8n nudges, so an advisor had to wait for an email to learn what needed doing. Added the in-product counterpart WITHOUT duplicating the analyzers: each cross-firm SQL constant now has a firm-scoped variant DERIVED from it by string-injecting the firm clause (STALE_ENGAGEMENTS_FIRM_SQL = STALE_ENGAGEMENTS_SQL.replace(...'and e.firm_id = $2'), same for tasks/reassess), so the firm query can never drift from the webhook query; each analyzer gained an optional firmId that selects the firm SQL + binds [days, firmId], and with no firmId it uses the exact cross-firm constant (webhook path byte-identical, existing tests unchanged). server/attention.ts firmAttention(db, firmId) composes the three into one payload {counts, reassessmentDue[], stalledTasks[], staleEngagements[]}. Registered as the firm-attention function under scope 'firm' (firm resolved from the caller's profile, never the body), read-only, not gated. Frontend: a "Needs attention" PageSection on the advisor dashboard (useFirmAttention) between the stat band and the engagements table, three grouped lists (reassessment due / stalled tasks [emphasised count] / gone quiet), each row a plain reason linking straight to the engagement, capped at 4 with "+N more"; hidden entirely when nothing is due (the stat band already shows the all-clear). Never writes a score or mutates an assessment (rules 4 & 5) — it reports that a re-assessment is due, it does not create one. Verified fresh in CI order: migrate → rls 87/87 → seed×2 → seed:demo → vitest 352 (+7: 5 firm-scoped analyzer unit tests locking the derived SQL + [days, firmId] binding and the unchanged webhook path; tests/attention.test.ts +2 DB integration proving all three signals fire and firm isolation — firm A's items never leak into firm B) → tsc -b + build clean, and driven live in a browser against the demo (injected a stale scenario: the panel rendered all three groups with plain reasons and clicking an item opened the engagement; no console errors).
- 2026-07-20 | Sign-up/footer/integration visual fixes (src/pages/SignUpPage.tsx, src/lib/clerkAppearance.ts, src/pages/LoginPage.tsx, src/main.tsx, src/App.tsx, src/styles.css, src/components/LegalFooter.tsx, src/pages/EngagementPage.tsx, src/pages/owner/OwnerHomePage.tsx) | Three reported visual issues on the sign-up/app chrome. (1) Clerk sign-up rendered unthemed: the app only mounted a themed <SignIn>, so clicking "Sign up" fell through to Clerk's default-light hosted Account Portal. Extracted the LoginPage appearance logic into shared src/lib/clerkAppearance.ts (clerkAppearance() reading the app's own CSS tokens + a useClerkAppearance() hook that recomputes on theme change), added a themed SignUpPage mounting Clerk's <SignUp routing="hash"> in our own login chrome, wired signUpUrl/signInUrl on ClerkProvider + both widgets so the flow stays in-app and themed in light/dark; off the Clerk stack (dev emulator) /sign-up redirects to /login (no self-serve there). No new product behavior — the sign-up link already existed; this just themes and in-apps it. (2) Legal footer crept up under content on short pages: wrapped Shell/OwnerShell content in a full-height flex column (.app-shell, min-height:100vh) with .page growing to absorb the leftover so the footer sits at the bottom; dropped the footer's now-redundant top margin (border-top + padding still separates it) which would otherwise overflow the viewport in the flex column. (3) QuickBooks/Xero accounting integration is not implemented yet, so removed it as an option "for now": dropped the advisor <AccountingCard> from EngagementPage, the "Connect" owner-nav link and the OwnerHomePage "Connect accounting" tile, and folded the /portal/connect route into a redirect to /portal. The AccountingCard/OwnerConnectPage components and the ledger server/tests are left intact so it can be re-enabled later. Presentation/IA only — no scoring/rubric/immutable-assessment/RLS/AI change. tsc -b + vite build clean, vitest 239 pass.
- 2026-07-20 | Per-engagement view-only external collaborators + firm-settings consolidation (migrations 20260720000400/500, server/collaborators.ts, registry `invite-collaborator`/`revoke-collaborator` + `manage-engagement` auth scope, server/clerk-webhook.ts, src/components/EngagementTeamCard.tsx, src/pages/SettingsPage.tsx, src/App.tsx, src/lib/{auth,owner,queries}.ts) | Two linked gaps Matthew flagged: (1) firm settings were managed in TWO places — ExitBlueprint (team/branding/billing) AND Clerk's Organization modal, which the Settings page linked out to ("Organization settings" → openOrganizationProfile), so membership/org-name lived in both; (2) there was no way to give a client's own CPA/attorney a read-only look at a deal — only the owner and firm staff could see anything. Fix (1): ExitBlueprint is now the single home for firm settings. Removed the "Organization settings" button; the Clerk card is reframed as personal-account-only (sign-in, password, MFA devices), and the firm's team is managed by the existing Team card, branding by the Branding card. Clerk stays the identity provider (person + membership), the app owns the firm — the split docs/24 §8 called for, now reflected in the UI. Fix (2): a new view-only `collaborator` app_role, scoped to a SINGLE engagement (not a company like the owner), assembled through the SAME owner-portal invite workflow. New engagement_collaborators roster (kind cpa|attorney|advisor|other, status invited|active|revoked) + a nullable profiles.engagement_id + an app.user_engagement_id() helper. RLS: collaborator read policies mirror the owner portal one-for-one but scope by engagement_id — everything a collaborator sees is a strict subset of what that engagement's owner sees (the one engagement, its company, completed assessments + scored children, gaps, tasks, owner report; firm name/branding via the existing role-less firm_member_read). No advisor/admin/staff policy matches role='collaborator', so no firm-scoped tenant data crosses engagements. The enum value is added in migration …400 and USED in …500 because migrate.ts commits each file separately (an enum value can't be added and used in one transaction — the same split the 'reviewer' role used). server/collaborators.ts inviteCollaborator/revokeCollaborator mirror inviteOwner's two-path shape (CLERK org invitation carrying app_role/engagement_id/company_id in public_metadata → provisioned by the membership-created webhook, which also flips the roster row to active; DEV direct-insert returns dev_password), registered under a new `manage-engagement` scope (firm staff advisor/admin + the engagement visible under RLS) in the collaboration engine, NOT gated (assembling a client's deal team is never blocked by billing). Frontend: RequireOwner generalised to RequirePortal (owner + collaborator both live in the read-only portal); RequireAdvisor/RequireStaff bounce collaborators to /portal; useOwnerContext resolves a collaborator by their pinned engagement_id (owners still resolve via company); a combined EngagementTeamCard on the Setup & admin tab (replacing OwnerAccessCard) shows the owner AND the view-only roster with an invite form (CPA/Attorney/Other) + revoke. Revoke deletes the collaborator's profile (RLS then denies every read) and marks the roster row revoked. No scoring/rubric/immutable-assessment change (rules 1–6 intact). Verified fresh in CI order on Postgres 16: migrate (both new files apply clean on a fresh DB, enum split works) → rls 94/94 (+7 collaborator: sees only its one engagement/company/completed assessments, read-only, no engagement log, only its firm branding, cannot read the roster — proving per-engagement isolation via a second engagement in the same company) → seed×2 → vitest 362 (+10: tests/collaborators.test.ts dev-path invite/revoke/idempotency/cross-firm, tests/functions.test.ts +2 manage-engagement 200 + non-staff 403, tests/clerk-webhook.test.ts +1 collaborator provisioning + roster activation, registry VALID_SCOPES +manage-engagement) → tsc -b + vite build clean.
- 2026-07-20 | Lead go-to-market with the CFP wedge (primary), CEPA/M&A adjacent; competitive positioning in docs/36 | CFPs are structurally underserved and capture the post-exit AUM event; follow-up: reconcile CLAUDE.md "distributed through M&A advisors" line (Matthew to ratify).
- 2026-07-20 | R5 secure document storage — Supabase Storage backend + real virus scanning behind opt-in env seams (server/documents/storage.ts, scanner.ts, pipeline.ts, engagements.ts, migration 20260720000600, DocumentsPage.tsx) | The docs pipeline's two deferred "R5 follow-up" stubs were the object-storage backend and the virus scan; encryption-at-rest, HMAC signed URLs, RLS, MFA, and the audit log had already shipped. Implemented both without touching any caller, defaults unchanged (db/noop) so CI and the dev emulator stay byte-identical. (1) StorageAdapter gained a SupabaseStorage backend (EB_STORAGE=supabase, lazy service-role client from SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY + EB_STORAGE_BUCKET): the SAME AES-256-GCM envelope we already store is uploaded as ciphertext to a private bucket keyed firm_id/document_id — deliberately keeping app-layer encryption + serve-through-our-function over Supabase's native at-rest + native signed URLs, because key custody stays in EB_DOCUMENT_KEY (a leaked object URL yields only ciphertext) and every read stays on the route that writes data_access_log (native signed URLs would bypass the audit trail). Trade-off accepted: bytes transit compute (fine at the 15 MB cap). (2) A ScannerAdapter seam (mirroring ParserAdapter, no npm dep) with NoopScanner (default, records scan_status='skipped'), a raw-clamd-INSTREAM ClamAVScanner (EB_SCANNER=clamav, fail-CLOSED on an unreachable daemon — never silently accept), and a FixtureScanner test seam (eicar→infected). (3) uploadDocument reordered to scan the plaintext BEFORE storage.put, so an infected file is recorded scan_status='infected' + status='rejected' and NEVER persisted (no malware object to clean up); a server-side extension allow-list (pdf/csv/txt/xls/xlsx/doc/docx/png/jpg/jpeg) sits by the size cap; a best-effort storage.remove compensates a bucket write a later txn failure can't roll back. (4) StorageAdapter.remove(db,{documentId,firmId}) added (path derived deterministically, since the documents row is already gone when it runs) and wired into deleteEngagement (snapshot doc ids pre-teardown, remove objects post-commit) — a bucket object does not cascade like document_blobs does. (5) Guarded migration 20260720000600 creates the private bucket only when the storage schema exists (no-op on the plain-Postgres shim). Frontend: client-side allow-list + size check before base64, an accept filter, a rejected-upload toast, and an "Infected" chip. No scoring/rubric/immutable-assessment change (rules 1–6 intact) — this is storage + scan hardening over the existing pipeline. Live Supabase-Storage/clamd verification is an ops step (needs a real bucket/daemon); offline the FixtureScanner exercises the full infected→rejected branch and defaults keep every existing test green.
- 2026-07-21 | Programs/Plans design blessed (docs/37); 7 product decisions resolved by Matthew — term "Plan"; seed scope both (system + firm-authored); owner-visible (owner-read RLS on instance tables); applying a playbook item generates tasks (reuses shared instantiation + once-per-engagement idempotency, tagged by engagement_plan_id); score-suggested (recommendation is a post-PL4 read-only slice); clone-to-customize yes; re-assessment reconcile (additive, PL4). No scoring/rubric/AI change; four-slice build (PL1 schema+seed → PL2 authoring → PL3 apply → PL4 tracking+reconcile).
- 2026-07-21 | Mobile optimization pass (index.html viewport-fit; src/styles.css "Mobile optimization pass" section; App.tsx AppNav) | The advisor workspace is desktop-first and had only piecemeal responsive rules, so a phone got a cramped horizontally-scrolling nav, iOS zoom-on-focus (15px body → sub-16px fields), 32px page gutters eating the narrow width, and card-grid's minmax(340px) forcing a column wider than the screen. Added one grouped mobile contract at the foot of styles.css (last so its overrides win): overflow-x:clip on the shell as a horizontal-overflow backstop (tables/gantt keep scrolling in their own boxes), safe-area insets on the sticky bar, 16px form fields under 680px to stop iOS auto-zoom, tighter page/card gutters, single-column collapse for fixed-min grids, and 44px touch targets under (pointer:coarse). The primary nav (both advisor and owner shells) collapses into a tap-to-open full-width sheet via a shared AppNav wrapper (hamburger toggle, closes on navigate/Escape/backdrop) — CSS switches inline-row↔sheet by width, no JS breakpoint. No product/scoring behavior changed; verified at 390px (no horizontal overflow, nav sheet + 16px inputs) via a static styles.css harness.
- 2026-07-21 | Advisor first-run activation checklist + onboarding empty-state polish (src/components/GettingStarted.tsx, src/pages/DashboardPage.tsx, src/styles.css) | docs/archive/35 flagged "thin first-run onboarding" — a brand-new advisor landed on an empty Engagements dashboard with only a single "Add engagement" CTA and dense DRS/ORI vocabulary explained nowhere. Added a "Getting started" PageSection on the dashboard that guides the three activation steps (secure the account with 2FA → start the first engagement → run the baseline assessment), each linking to where the work already happens (Settings, the add-engagement dialog, the engagement page) rather than a bespoke wizard. It reads the signals the dashboard already has plus one getMfaState() lookup, marks done/active/locked per step, and self-dismisses once the firm records its first assessment (assessed.length>0) so an established book never sees it. Also enriched the empty-book copy with a one-line DRS/ORI explainer and removed a CLI instruction that leaked into the UI (the no-agreement ErrorState told end users to run "npm run admin -- create-agreement-version"; now points them to their administrator). Presentation/onboarding only — no scoring/rubric/immutable-assessment/RLS/AI change (rules 1–6 intact); owner self-signup stays out of scope. tsc -b + vite build clean, vitest 247 pass.
- 2026-07-21 | Hosted sales-demo tenant is a one-command orchestration of existing seeds/CLI, not new product code (scripts/sales-demo.sh, npm run demo:sales, docs/37) | A stakeholder needs a live tenant to demo in sales, signing in from any laptop, seeing both the advisor workspace and the client portal on one engagement. The pieces already existed — seed:demo (deep Cascade client), seed:portfolio (book of business), and admin create-firm/create-advisor/assign-company (Clerk provisioning) — but only dev-demo.sh wired them together, and only for a throwaway LOCAL Postgres + dev-emulator login. sales-demo.sh orchestrates the same idempotent, firm-scoped steps against an already-deployed DB with Clerk: create-firm (+ org) → advisor login → seed:demo → seed:portfolio → owner login → assign-company (scopes owner to Cascade so /portal resolves the engagement). Two logins are named at run time (plus-alias one inbox); owner is provisioned directly (membership + profile + company_id) so no invite email/webhook is needed to sign in. No scoring/rubric/schema/RLS/AI change; never migrates or seeds methodology (assumes a deployed DB) and touches only the demo firm.
- 2026-07-21 | Mobile optimization pass 2 — tabs, forms, overflow (src/styles.css "Mobile optimization pass" section + inline .val-inputs collapse + body overflow-wrap; src/components/ui/SubTabs.tsx, src/components/ui/EngagementNav.tsx) | Follow-up to #90 on three phone papercuts Matthew reported after the first pass. (1) Tabs awkwardly placed: both the engagement tab bar (.eng-nav) and the in-page panel switcher (.subtabs) were laid out to wrap, so on a 390px phone 6–7 tabs stacked into a 2–3 row block (the .subtabs pill container looked especially bad wrapped). Under 680px both become single-row horizontal-scroll strips (flex-wrap:nowrap; overflow-x:auto; hidden scrollbar; scroll-snap; tabs flex:0 0 auto) — the partially-clipped last tab is the "more →" affordance, the standard mobile tab pattern. SubTabs/EngagementNav gained a small effect that scrollIntoView({inline:'center'})s the active tab on selection/navigation so deep tabs (Valuation, Delta report, Setup & admin) aren't left scrolled off-screen. Desktop is untouched (rules are mobile-only; .eng-nav overflowX stays visible). (2) Longer form text didn't fit: the valuation Assumptions grid (.val-inputs, 2-col) never collapsed, so the second column (Multiple override, Owner's target) hung ~220px off the right edge; added an inline 560px collapse to one column + min-width:0 on its fields, matching the collapse pattern its sibling val grids already use. Also added overflow-wrap:break-word on body so a long unbroken token (email, URL, filename, id) wraps instead of forcing the page wider. (3) Tables/rows off screen: .assessment-card packed a seq + score + date + two links into one non-wrapping flex row and spilled ~15px past a phone; it now wraps under 560px with the score taking a full row so the links drop to a second line. Presentation only — no product/scoring/rubric/immutable-assessment/RLS/AI change (rules 1–6 intact). Verified live in the demo at 390px via Playwright (mobile emulation): every reported surface (engagement Overview subtabs, Valuation eng-nav + Assumptions, Delta/Setup nav, assessment record) has no element past the viewport, active tab centers into view, and desktop 1280px keeps the normal wrapped rows; tsc -b clean, vitest 247 pass.
